# EvoAgent 生产部署手册（Docker Hub + VPS + Caddy 反代）

适用：把本地 `coding-evoagent` 镜像推到 Docker Hub，在任意一台装了 Docker 的 VPS 上用 `docker compose` 拉起
postgres + redis + evoagent，并通过 Caddy 做 HTTPS 反代。当前未绑域名，使用 Caddy 自签名证书；
后续绑域名可一键切到 Let's Encrypt。

---

## 一、本机（推送镜像到 Docker Hub）

1. 填写生产环境变量模板：
   ```powershell
   Copy-Item .env.prod.example .env.prod
   # 用编辑器打开 .env.prod，至少改掉：POSTGRES_PASSWORD、EVOAGENT_AUTH_SECRET、EVOAGENT_BOOTSTRAP_ADMIN_PASSWORD、EVOAGENT_DEEPSEEK_API_KEY
   ```
   生成随机 AUTH_SECRET：
   ```powershell
   python -c "import secrets;print(secrets.token_urlsafe(32))"
   ```

2. 登录 Docker Hub（只需一次）：
   ```powershell
   docker login
   ```

3. 构建并推送镜像：
   ```powershell
   .\push_image.ps1 -User 你的DockerHub用户名 -Tag latest
   ```
   推送成功后，镜像名为 `你的DockerHub用户名/coding-evoagent:latest`。

---

## 二、VPS 前置条件

- 一台 Linux VPS（Ubuntu 22.04 / 24.04 或 CentOS 7+，2 核 4G 起），已装 Docker 与 Docker Compose v2 插件：
  ```bash
  # Ubuntu 示例
  sudo apt-get update && sudo apt-get install -y docker.io docker-compose-plugin
  sudo systemctl enable --now docker
  ```
- 防火墙放行 **443**（以及后续用 Let's Encrypt 时的 **80**）：
  ```bash
  sudo ufw allow 443/tcp
  sudo ufw allow 80/tcp      # 绑定域名后再开也可以
  sudo ufw enable
  ```
- 在 VPS 上登录 Docker Hub（拉取私有/公开镜像）：
  ```bash
  docker login
  ```

---

## 三、部署

1. 把以下文件传到 VPS 同一目录（例如 `~/evoagent/`）：
   - `docker-compose.prod.yml`
   - `Caddyfile`
   - `.env.prod`（已填好真实值，**不要**提交到 git）

2. 确认 `.env.prod` 里的 `EVOAGENT_IMAGE` 与推送的镜像名一致。

3. 启动：
   ```bash
   docker compose --env-file .env.prod -f docker-compose.prod.yml up -d
   ```

4. 查看状态：
   ```bash
   docker compose -f docker-compose.prod.yml ps
   docker compose -f docker-compose.prod.yml logs -f evoagent
   ```

---

## 四、验证

- 健康检查（忽略自签名证书告警用 `-k`）：
  ```bash
  curl -k https://localhost/health
  # 期望返回 {"status":"ok"}
  ```
- 浏览器打开 `https://<你的VPS公网IP>`：
  - 首次访问会提示证书不安全，点“继续/高级→继续”即可（自签名所致）。
  - 用 `.env.prod` 里的管理员账号登录，进入“问题记忆库”等页面。

---

## 五、后续绑定域名（可选，推荐）

1. 在域名服务商把 `A 记录` 指向 VPS 公网 IP。
2. 编辑 `Caddyfile`：
   ```
   review.example.com {
       tls you@email.com
       reverse_proxy evoagent:8080
   }
   ```
   （把 `:443 { tls internal ... }` 整段替换为上面这段）
3. 确认防火墙已开 80：
   ```bash
   sudo ufw allow 80/tcp
   ```
4. 重新加载：
   ```bash
   docker compose -f docker-compose.prod.yml restart caddy
   ```
   Caddy 会自动向 Let's Encrypt 申请并续期证书，浏览器不再告警。

---

## 六、数据持久化与备份

- 任务、审查报告、记忆库（findings_bank）都存在 postgres 的命名卷 `postgres_data` 中；
  evoagent 自身数据在 `evoagent_data`。容器重建不会丢数据。
- 备份 postgres：
  ```bash
  docker compose -f docker-compose.prod.yml exec -T postgres pg_dump -U evoagent evoagent > backup_$(date +%F).sql
  ```
- 恢复：
  ```bash
  docker compose -f docker-compose.prod.yml exec -T postgres psql -U evoagent evoagent < backup_YYYY-MM-DD.sql
  ```

---

## 七、升级

1. 本机改代码后重新推送（建议用新的 Tag，如 `v1.1`）：
   ```powershell
   .\push_image.ps1 -User 你的DockerHub用户名 -Tag v1.1
   ```
2. VPS 上改 `.env.prod` 的 `EVOAGENT_IMAGE` 为 `...:v1.1`，然后：
   ```bash
   docker compose --env-file .env.prod -f docker-compose.prod.yml pull
   docker compose --env-file .env.prod -f docker-compose.prod.yml up -d
   ```

---

## 八、安全提示（重要）

- 当前把整个管理台通过 HTTPS 暴露到公网。演示/答辩可以，但**长期不建议**公网开放管理台。
- 建议：演示结束后把 VPS 防火墙改成仅放行你自己的 IP，或临时 `docker compose ... stop caddy`。
- 务必使用强 `EVOAGENT_AUTH_SECRET` 与管理员密码，不要用示例占位符。
- 若只做 GitHub PR 审查、不需要公网访问管理台，可按项目 README 的建议，用反向代理只暴露 `/webhooks/github` 与 `/health`。
