# EvoAgent 量化代码审查

**Repository:** `聚宽2021/机器学习模型大比拼!`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 12 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `8`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `189 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`机器学习模型大比拼!.py:164` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
is_paused = get_price(stocks,end_date=beginDate, count=1,fields='paused',panel=False)
```

**证据引用：** `local-rule:2456c177b88c65a6`, `diff-ast:78457a95dc587eb6`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`机器学习模型大比拼!.py:432` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
close_start = get_price(stocklist, count = 1, end_date=start,fields='close',panel=False).set_index('code')[['close']]
```

**证据引用：** `local-rule:c9b69571b037e663`, `diff-ast:76412b47fade0a4e`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`机器学习模型大比拼!.py:433` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
close_end = get_price(stocklist, count = 1, end_date=end,fields='close',panel=False).set_index('code')[['close']]
```

**证据引用：** `local-rule:1ca9f5517846a2e6`, `diff-ast:0363ea3cf9a12f4a`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`机器学习模型大比拼!.py:802` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
bench = get_price(INDEX,date,trade_days_test[idx+1],fields=['close'],panel=False)['close']
```

**证据引用：** `local-rule:5f22b2aab8bd2d09`, `diff-ast:501486d7f53ded5c`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🟠 网格寻优需使用时序交叉验证避免数据窥探

`机器学习模型大比拼!.py:846` · **MEDIUM** · `QUANT-OF-GRIDSEARCH`

对时间序列用普通 K 折会把未来折的信息泄漏给训练折。

**证据**

```text
# knn_GS = GridSearchCV(estimator=knn,param_grid=params,cv=5)
```

**证据引用：** `local-rule:ab0e2070c5cd3b74`

**修复建议：** 改用 TimeSeriesSplit / Purged K-Fold 等时序交叉验证。

**测试建议：** 断言交叉验证切分保持时间顺序且测试折晚于训练折。

### 6. 🟠 网格寻优需使用时序交叉验证避免数据窥探

`机器学习模型大比拼!.py:916` · **MEDIUM** · `QUANT-OF-GRIDSEARCH`

对时间序列用普通 K 折会把未来折的信息泄漏给训练折。

**证据**

```text
lg_GS = GridSearchCV(estimator=lg,param_grid=params,cv=5)
```

**证据引用：** `local-rule:2bed227f111abb2d`

**修复建议：** 改用 TimeSeriesSplit / Purged K-Fold 等时序交叉验证。

**测试建议：** 断言交叉验证切分保持时间顺序且测试折晚于训练折。

### 7. 🟠 网格寻优需使用时序交叉验证避免数据窥探

`机器学习模型大比拼!.py:994` · **MEDIUM** · `QUANT-OF-GRIDSEARCH`

对时间序列用普通 K 折会把未来折的信息泄漏给训练折。

**证据**

```text
tree_GS = GridSearchCV(estimator=tree,param_grid=params,cv=5)
```

**证据引用：** `local-rule:51d73f3d7bdf0021`

**修复建议：** 改用 TimeSeriesSplit / Purged K-Fold 等时序交叉验证。

**测试建议：** 断言交叉验证切分保持时间顺序且测试折晚于训练折。

### 8. 🟠 网格寻优需使用时序交叉验证避免数据窥探

`机器学习模型大比拼!.py:1131` · **MEDIUM** · `QUANT-OF-GRIDSEARCH`

对时间序列用普通 K 折会把未来折的信息泄漏给训练折。

**证据**

```text
rfc_GS = GridSearchCV(estimator=rfc,param_grid=params,cv=5)
```

**证据引用：** `local-rule:3ff368fa96345123`

**修复建议：** 改用 TimeSeriesSplit / Purged K-Fold 等时序交叉验证。

**测试建议：** 断言交叉验证切分保持时间顺序且测试折晚于训练折。

### 9. 🟠 网格寻优需使用时序交叉验证避免数据窥探

`机器学习模型大比拼!.py:1211` · **MEDIUM** · `QUANT-OF-GRIDSEARCH`

对时间序列用普通 K 折会把未来折的信息泄漏给训练折。

**证据**

```text
adb_GS = GridSearchCV(estimator=adb,param_grid=params,cv=5)
```

**证据引用：** `local-rule:7ec8840e99ff9990`

**修复建议：** 改用 TimeSeriesSplit / Purged K-Fold 等时序交叉验证。

**测试建议：** 断言交叉验证切分保持时间顺序且测试折晚于训练折。

### 10. 🟡 非交互式回测中调用 plt.show()

`机器学习模型大比拼!.py:826` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:机器学习模型大比拼!.py:826`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 11. 🟡 非交互式回测中调用 plt.show()

`机器学习模型大比拼!.py:1402` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:机器学习模型大比拼!.py:1402`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 12. 🟡 非交互式回测中调用 plt.show()

`机器学习模型大比拼!.py:1442` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:机器学习模型大比拼!.py:1442`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

