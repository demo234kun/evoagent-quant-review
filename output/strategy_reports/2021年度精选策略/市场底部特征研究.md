# EvoAgent 量化代码审查

**Repository:** `聚宽2021/市场底部特征研究`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 7 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `10`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `241 ms`

## 审查发现

### 1. 🔴 策略代码中出现 eval/exec

`市场底部特征研究.py:25` · **HIGH** · `QUANT-CQ-UNSAFE-EVAL`

在量化脚本里执行动态代码存在注入与不可复现风险，且难以做静态审查。

**证据**

```text
ax.plot(eval('df.'+x),df['000001.XSHG'],color='b',label='上证综指')
```

**证据引用：** `scanner:QUANT-CQ-UNSAFE-EVAL:市场底部特征研究.py:25`, `diff-ast:7e9169f721e0ce86`

**修复建议：** 改为显式函数与配置驱动的参数解析。

**测试建议：** 验证移除 eval/exec 后策略逻辑等价且可静态审查。

### 2. 🔴 策略代码中出现 eval/exec

`市场底部特征研究.py:28` · **HIGH** · `QUANT-CQ-UNSAFE-EVAL`

在量化脚本里执行动态代码存在注入与不可复现风险，且难以做静态审查。

**证据**

```text
ax1.plot(eval('df.'+x),df[param],color='y',label=param)
```

**证据引用：** `scanner:QUANT-CQ-UNSAFE-EVAL:市场底部特征研究.py:28`, `diff-ast:38caecbe14051083`

**修复建议：** 改为显式函数与配置驱动的参数解析。

**测试建议：** 验证移除 eval/exec 后策略逻辑等价且可静态审查。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`市场底部特征研究.py:129` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price('000001.XSHG',start_date=i+'-9',end_date=i+'-20',fields=['close'],fq=None)
```

**证据引用：** `local-rule:5465fae3fd38b9bc`, `diff-ast:cc30a1881c7855cb`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`市场底部特征研究.py:280` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df['fir_open']=[get_price(stock,end_date=df.loc[stock]['start_date'],count=1,fields=['open'])['open'].values[0] for stock in df.index]
```

**证据引用：** `local-rule:dec8dad7eebbfeeb`, `diff-ast:a0fce12d8466f24a`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🔴 策略代码中出现 eval/exec

`市场底部特征研究.py:292` · **HIGH** · `QUANT-CQ-UNSAFE-EVAL`

在量化脚本里执行动态代码存在注入与不可复现风险，且难以做静态审查。

**证据**

```text
df3=df[pd.eval('df.start_date<date') & pd.eval('df.start_date>date2')]
```

**证据引用：** `scanner:QUANT-CQ-UNSAFE-EVAL:市场底部特征研究.py:292`, `diff-ast:afe95968bf10bc3c`

**修复建议：** 改为显式函数与配置驱动的参数解析。

**测试建议：** 验证移除 eval/exec 后策略逻辑等价且可静态审查。

### 6. 🟡 非交互式回测中调用 plt.show()

`市场底部特征研究.py:31` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:市场底部特征研究.py:31`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 7. 🟡 非交互式回测中调用 plt.show()

`市场底部特征研究.py:338` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:市场底部特征研究.py:338`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

