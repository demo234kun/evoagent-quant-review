# EvoAgent 量化代码审查

**Repository:** `聚宽2021/机器学习用于量化分析`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 15 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `11`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `194 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`机器学习用于量化分析.py:254` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
stock_data = get_price('000001.XSHE',start_date,end_date,'daily',fields=['close'])
```

**证据引用：** `local-rule:ad7bf803a75bd7a4`, `diff-ast:76422e498ac8df8d`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`机器学习用于量化分析.py:544` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
data_close=get_price(stockList,date,dateList[dateList.index(date)+1],'1d','close')['close']
```

**证据引用：** `local-rule:dba9311ccb8deb5f`, `diff-ast:183bfa6f545d7f12`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`机器学习用于量化分析.py:590` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
data_close=get_price(stockList,date,dateList[dateList.index(date)+1],'1d','close')['close']
```

**证据引用：** `local-rule:e6f665a91214ae99`, `diff-ast:2d8704cce0dffcdc`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 在全样本上 fit_transform 造成数据泄露

`机器学习用于量化分析.py:822` · **HIGH** · `QUANT-LK-FULL-NORM`

在训练/测试切分之前对整个数据集 fit_transform，会让测试集的统计信息泄漏进训练。

**证据**

```text
res = model.fit_transform(data_x,data_y)
```

**证据引用：** `local-rule:dc1126c01085e862`, `diff-ast:7aa20b0cdeb650f2`

**修复建议：** 先切分再分别在训练集 fit，并用同一 scaler 仅 transform 测试集。

**测试建议：** 对比全样本缩放与时序切分缩放下的测试集指标，断言两者不同且后者更保守。

### 5. 🔴 在全样本上 fit_transform 造成数据泄露

`机器学习用于量化分析.py:826` · **HIGH** · `QUANT-LK-FULL-NORM`

在训练/测试切分之前对整个数据集 fit_transform，会让测试集的统计信息泄漏进训练。

**证据**

```text
res = model.fit_transform(data_x,data_y)
```

**证据引用：** `local-rule:0c7c212ab1058f63`, `diff-ast:8e1bfab63571249c`

**修复建议：** 先切分再分别在训练集 fit，并用同一 scaler 仅 transform 测试集。

**测试建议：** 对比全样本缩放与时序切分缩放下的测试集指标，断言两者不同且后者更保守。

### 6. 🔴 在全样本上 fit_transform 造成数据泄露

`机器学习用于量化分析.py:835` · **HIGH** · `QUANT-LK-FULL-NORM`

在训练/测试切分之前对整个数据集 fit_transform，会让测试集的统计信息泄漏进训练。

**证据**

```text
res = model.fit_transform(data_x,data_y)
```

**证据引用：** `local-rule:29643b848572f3d0`, `diff-ast:cbdc2d43000a5467`

**修复建议：** 先切分再分别在训练集 fit，并用同一 scaler 仅 transform 测试集。

**测试建议：** 对比全样本缩放与时序切分缩放下的测试集指标，断言两者不同且后者更保守。

### 7. 🔴 在全样本上 fit_transform 造成数据泄露

`机器学习用于量化分析.py:853` · **HIGH** · `QUANT-LK-FULL-NORM`

在训练/测试切分之前对整个数据集 fit_transform，会让测试集的统计信息泄漏进训练。

**证据**

```text
res = model.fit_transform(data_x,data_y)
```

**证据引用：** `local-rule:7b6fdfc7dcbf250a`, `diff-ast:6982a2936882e7be`

**修复建议：** 先切分再分别在训练集 fit，并用同一 scaler 仅 transform 测试集。

**测试建议：** 对比全样本缩放与时序切分缩放下的测试集指标，断言两者不同且后者更保守。

### 8. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`机器学习用于量化分析.py:164` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
securities_list = delete_stop(get_index_stocks(SELECT_STOCK_INDEX),start_date,DELETE_STOP_NDAYS)
```

**证据引用：** `local-rule:7b1720f778032801`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 9. 🟠 裸异常吞掉错误

`机器学习用于量化分析.py:445` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:d0670b5a96840a7f`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 10. 🟠 裸异常吞掉错误

`机器学习用于量化分析.py:503` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:51fb6c7f207e87de`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 11. 🟠 网格寻优需使用时序交叉验证避免数据窥探

`机器学习用于量化分析.py:937` · **MEDIUM** · `QUANT-OF-GRIDSEARCH`

对时间序列用普通 K 折会把未来折的信息泄漏给训练折。

**证据**

```text
rf_grid_serach_model = GridSearchCV(estimator=rf_model,param_grid=model_params)
```

**证据引用：** `local-rule:6ec1d5f475456f3a`

**修复建议：** 改用 TimeSeriesSplit / Purged K-Fold 等时序交叉验证。

**测试建议：** 断言交叉验证切分保持时间顺序且测试折晚于训练折。

### 12. 🟠 网格寻优需使用时序交叉验证避免数据窥探

`机器学习用于量化分析.py:948` · **MEDIUM** · `QUANT-OF-GRIDSEARCH`

对时间序列用普通 K 折会把未来折的信息泄漏给训练折。

**证据**

```text
rf_grid_serach_model = GridSearchCV(estimator=rf_model,param_grid=model_params)
```

**证据引用：** `local-rule:d68a5f3740d4a1a8`

**修复建议：** 改用 TimeSeriesSplit / Purged K-Fold 等时序交叉验证。

**测试建议：** 断言交叉验证切分保持时间顺序且测试折晚于训练折。

### 13. 🟠 网格寻优需使用时序交叉验证避免数据窥探

`机器学习用于量化分析.py:1006` · **MEDIUM** · `QUANT-OF-GRIDSEARCH`

对时间序列用普通 K 折会把未来折的信息泄漏给训练折。

**证据**

```text
model = GridSearchCV(estimator=xgboost_model,param_grid=model_params,cv=5)
```

**证据引用：** `local-rule:3293ec57f5a5bce3`

**修复建议：** 改用 TimeSeriesSplit / Purged K-Fold 等时序交叉验证。

**测试建议：** 断言交叉验证切分保持时间顺序且测试折晚于训练折。

### 14. 🟡 非交互式回测中调用 plt.show()

`机器学习用于量化分析.py:884` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:机器学习用于量化分析.py:884`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 15. 🟡 非交互式回测中调用 plt.show()

`机器学习用于量化分析.py:911` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:机器学习用于量化分析.py:911`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

