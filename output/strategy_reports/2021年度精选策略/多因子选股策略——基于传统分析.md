# EvoAgent 量化代码审查

**Repository:** `聚宽2021/多因子选股策略——基于传统分析`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 15 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `10`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `195 ms`

## 审查发现

### 1. 🔴 策略逻辑使用了真实当前时间

`多因子选股策略——基于传统分析.py:194` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
current_date = datetime.datetime.now()
```

**证据引用：** `local-rule:97e1228eeffb85b5`, `diff-ast:2797739c2e4859a7`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股策略——基于传统分析.py:426` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price('000001.XSHG', end_date=end, count=count)
```

**证据引用：** `local-rule:ba6144f9a470119c`, `diff-ast:b855d6772067a478`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股策略——基于传统分析.py:428` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price('000001.XSHG', start_date=start, end_date=end)
```

**证据引用：** `local-rule:1a8cbaacd8813237`, `diff-ast:cd0cf708cb709c40`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股策略——基于传统分析.py:893` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
start_price = get_price(stocks,end_date=start_date,count=1,fields=['close'])['close']
```

**证据引用：** `local-rule:839218811500e5ba`, `diff-ast:3b358d75cae9a1b2`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股策略——基于传统分析.py:896` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
end_price = get_price(stocks,end_date=end_date,count=1,fields=['close'])['close']
```

**证据引用：** `local-rule:2d9c4b2e7e5dedbc`, `diff-ast:47a76a3c4d176342`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 6. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股策略——基于传统分析.py:905` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
end_price = get_price(stocks,end_date=end_date,count=1,fields=['close'])['close']
```

**证据引用：** `local-rule:b9045c9c1732bf72`, `diff-ast:39e8cbf0f7d78ba1`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 7. 🟠 使用 statDate 拉取财报期数据，可能包含未披露数据

`多因子选股策略——基于传统分析.py:356` · **MEDIUM** · `QUANT-LK-STATDATE`

用 statDate 指定报告期会在报告实际发布前取到数据，形成前视。

**证据**

```text
fundamental_data = get_fundamentals(ttm_q, statDate=statdate)
```

**证据引用：** `local-rule:0659f397f716eb10`

**修复建议：** 用 point-in-time 的发布日字段，或在回测日之前取最近已披露财报。

**测试建议：** 断言取到的财报披露日不晚于回测当前日。

### 8. 🟠 使用 statDate 拉取财报期数据，可能包含未披露数据

`多因子选股策略——基于传统分析.py:360` · **MEDIUM** · `QUANT-LK-STATDATE`

用 statDate 指定报告期会在报告实际发布前取到数据，形成前视。

**证据**

```text
fundamental_data = get_fundamentals(ttm_q, statDate=statdate)
```

**证据引用：** `local-rule:57c7a750c5915554`

**修复建议：** 用 point-in-time 的发布日字段，或在回测日之前取最近已披露财报。

**测试建议：** 断言取到的财报披露日不晚于回测当前日。

### 9. 🟠 使用 statDate 拉取财报期数据，可能包含未披露数据

`多因子选股策略——基于传统分析.py:389` · **MEDIUM** · `QUANT-LK-STATDATE`

用 statDate 指定报告期会在报告实际发布前取到数据，形成前视。

**证据**

```text
fund_data = get_fundamentals(q, statDate=date)
```

**证据引用：** `local-rule:a3a40a1d7bfa38e6`

**修复建议：** 用 point-in-time 的发布日字段，或在回测日之前取最近已披露财报。

**测试建议：** 断言取到的财报披露日不晚于回测当前日。

### 10. 🟠 使用 statDate 拉取财报期数据，可能包含未披露数据

`多因子选股策略——基于传统分析.py:483` · **MEDIUM** · `QUANT-LK-STATDATE`

用 statDate 指定报告期会在报告实际发布前取到数据，形成前视。

**证据**

```text
fund_data = get_fundamentals(q, statDate=date)
```

**证据引用：** `local-rule:8723c548f70a12d6`

**修复建议：** 用 point-in-time 的发布日字段，或在回测日之前取最近已披露财报。

**测试建议：** 断言取到的财报披露日不晚于回测当前日。

### 11. 🟠 使用 statDate 拉取财报期数据，可能包含未披露数据

`多因子选股策略——基于传统分析.py:494` · **MEDIUM** · `QUANT-LK-STATDATE`

用 statDate 指定报告期会在报告实际发布前取到数据，形成前视。

**证据**

```text
fund_data = get_fundamentals(q, statDate=date)
```

**证据引用：** `local-rule:98373716eaf810f7`

**修复建议：** 用 point-in-time 的发布日字段，或在回测日之前取最近已披露财报。

**测试建议：** 断言取到的财报披露日不晚于回测当前日。

### 12. 🟠 裸异常吞掉错误

`多因子选股策略——基于传统分析.py:662` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:94f98f009c1bd224`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 13. 🟡 用 0 填补缺失值可能扭曲收益率

`多因子选股策略——基于传统分析.py:568` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
df = df.fillna(0)
```

**证据引用：** `local-rule:31bfc3b825493e32`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 14. 🟡 非交互式回测中调用 plt.show()

`多因子选股策略——基于传统分析.py:1018` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:多因子选股策略——基于传统分析.py:1018`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 15. 🟡 非交互式回测中调用 plt.show()

`多因子选股策略——基于传统分析.py:1157` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:多因子选股策略——基于传统分析.py:1157`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

