# EvoAgent 量化代码审查

**Repository:** `聚宽2021/【复现】华泰证券-波动率和换手率构建牛熊指标`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 18 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `10`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `204 ms`

## 审查发现

### 1. 🚨 回测使用未来数据 (shift(-N))

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:637` · **CRITICAL** · `QUANT-FF-SHIFT-NEG`

用 df.shift(-1) 等把下一根 bar 的数据对齐到当前行，会让信号偷看到未发生的价格，回测收益虚高。

**证据**

```text
df['未来1日收益率'] = df['pct_chg'].shift(-1)  # next_ret
```

**证据引用：** `local-rule:e4f6d7cb5f284fdd`, `diff-ast:0b5c216d98ca72df`

**修复建议：** 需要用到未来信息时改为 shift(正数) 或显式构造下一根标签；因子与信号必须只用当前及之前的数据。

**测试建议：** 构造一个已知结果的DataFrame，断言信号行不会引用其后的价格。

### 2. 🚨 回测使用未来数据 (shift(-N))

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:1048` · **CRITICAL** · `QUANT-FF-SHIFT-NEG`

用 df.shift(-1) 等把下一根 bar 的数据对齐到当前行，会让信号偷看到未发生的价格，回测收益虚高。

**证据**

```text
back_df['NEXT_RET'] = back_df['pct_chg'].shift(-1)
```

**证据引用：** `local-rule:3cbd9ff78900a16a`, `diff-ast:d90c7b010d1995f4`

**修复建议：** 需要用到未来信息时改为 shift(正数) 或显式构造下一根标签；因子与信号必须只用当前及之前的数据。

**测试建议：** 构造一个已知结果的DataFrame，断言信号行不会引用其后的价格。

### 3. 🚨 回测使用未来数据 (shift(-N))

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:1120` · **CRITICAL** · `QUANT-FF-SHIFT-NEG`

用 df.shift(-1) 等把下一根 bar 的数据对齐到当前行，会让信号偷看到未发生的价格，回测收益虚高。

**证据**

```text
df['NEXT_RET'] = df['pct_chg'].shift(-1)
```

**证据引用：** `local-rule:0575b94836fa5a62`, `diff-ast:60ac9c8dc9129436`

**修复建议：** 需要用到未来信息时改为 shift(正数) 或显式构造下一根标签；因子与信号必须只用当前及之前的数据。

**测试建议：** 构造一个已知结果的DataFrame，断言信号行不会引用其后的价格。

### 4. 🚨 回测使用未来数据 (shift(-N))

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:1151` · **CRITICAL** · `QUANT-FF-SHIFT-NEG`

用 df.shift(-1) 等把下一根 bar 的数据对齐到当前行，会让信号偷看到未发生的价格，回测收益虚高。

**证据**

```text
pre_mark = np.nan_to_num(df[mark_col].shift(-1))
```

**证据引用：** `local-rule:9eaea1726c6140ee`, `diff-ast:2e9d9a9721e14ad5`

**修复建议：** 需要用到未来信息时改为 shift(正数) 或显式构造下一根标签；因子与信号必须只用当前及之前的数据。

**测试建议：** 构造一个已知结果的DataFrame，断言信号行不会引用其后的价格。

### 5. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:126` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
close_df = get_price('000001.XSHG', start, end, fields=['close', 'pre_close'])
```

**证据引用：** `local-rule:910ed35a0f538442`, `diff-ast:3bd5e6e344c5cde3`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 6. 🟡 策略中存在阻塞式 sleep

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:99` · **LOW** · `QUANT-CQ-BLOCKING-SLEEP`

在回测或实盘策略热路径里调用 time.sleep 会阻塞事件循环或下单线程，导致漏单与超时。

**证据**

```text
time.sleep(1)
```

**证据引用：** `scanner:QUANT-CQ-BLOCKING-SLEEP:【复现】华泰证券-波动率和换手率构建牛熊指标.py:99`

**修复建议：** 改为基于事件/定时器的调度，或将等待逻辑移出策略核心。

**测试建议：** 验证移除 sleep 后策略仍能按预期频率触发。

### 7. 🟡 非交互式回测中调用 plt.show()

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:143` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:【复现】华泰证券-波动率和换手率构建牛熊指标.py:143`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 8. 🟡 非交互式回测中调用 plt.show()

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:178` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:【复现】华泰证券-波动率和换手率构建牛熊指标.py:178`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 9. 🟡 非交互式回测中调用 plt.show()

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:206` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:【复现】华泰证券-波动率和换手率构建牛熊指标.py:206`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 10. 🟡 非交互式回测中调用 plt.show()

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:254` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:【复现】华泰证券-波动率和换手率构建牛熊指标.py:254`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 11. 🟡 非交互式回测中调用 plt.show()

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:283` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:【复现】华泰证券-波动率和换手率构建牛熊指标.py:283`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 12. 🟡 非交互式回测中调用 plt.show()

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:314` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:【复现】华泰证券-波动率和换手率构建牛熊指标.py:314`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 13. 🟡 非交互式回测中调用 plt.show()

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:360` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:【复现】华泰证券-波动率和换手率构建牛熊指标.py:360`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 14. 🟡 非交互式回测中调用 plt.show()

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:398` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:【复现】华泰证券-波动率和换手率构建牛熊指标.py:398`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 15. 🟡 非交互式回测中调用 plt.show()

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:685` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:【复现】华泰证券-波动率和换手率构建牛熊指标.py:685`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 16. 🟡 非交互式回测中调用 plt.show()

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:727` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:【复现】华泰证券-波动率和换手率构建牛熊指标.py:727`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 17. 🟡 非交互式回测中调用 plt.show()

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:1072` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:【复现】华泰证券-波动率和换手率构建牛熊指标.py:1072`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 18. 🟡 非交互式回测中调用 plt.show()

`【复现】华泰证券-波动率和换手率构建牛熊指标.py:1293` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
return plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:【复现】华泰证券-波动率和换手率构建牛熊指标.py:1293`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

