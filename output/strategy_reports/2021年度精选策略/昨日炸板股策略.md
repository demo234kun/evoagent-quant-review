# EvoAgent 量化代码审查

**Repository:** `聚宽2021/昨日炸板股策略`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 8 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `10`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `168 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`昨日炸板股策略.py:56` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price(securities, fields=['open','high','low','close','high_limit','paused'], end_date=end_date, count=1, panel=False).set_index('code', drop=True)
```

**证据引用：** `local-rule:f0ae2403239bbf21`, `diff-ast:88d918112a4b6986`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`昨日炸板股策略.py:69` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_tmp = get_price(g.stock_pool, end_date=date, frequency='1m', count=240, fields=['close','high_limit'], panel=False)
```

**证据引用：** `local-rule:d87934a9948103ab`, `diff-ast:7cfddedb06e8b206`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`昨日炸板股策略.py:77` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_idx = get_price(g.stop_loss_index, fields='close', end_date=end_date, count=5, panel=False)
```

**证据引用：** `local-rule:dfd3c025f35abff2`, `diff-ast:4a0ac53d912c3f14`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`昨日炸板股策略.py:92` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_idx = get_price('000001.XSHG', end_date=end_date, count=35, fields='close')
```

**证据引用：** `local-rule:d4d61b5e5ede5e02`, `diff-ast:5a2f59065ddaf1d5`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🔴 行情接口以当前时刻为结束日（包含未发生的 bar）

`昨日炸板股策略.py:138` · **HIGH** · `QUANT-FF-GETPRICE-NOW`

JoinQuant 的 get_price/attribute_history 在 end_date=context.current_dt 时会把当根 bar 也算入，若据此下单即为未来函数。

**证据**

```text
df = get_price(stock, start_date=g.run_time, end_date=context.current_dt, frequency='1m', fields=['close','high', 'high_limit'], panel=False)
```

**证据引用：** `local-rule:8a1334890b7ac268`, `diff-ast:67743db5ae603f57`

**修复建议：** 用 context.previous_date 作为结束日，或改用 history()（默认不含当根）。

**测试建议：** 断言行情查询的结束日早于当前信号 bar。

### 6. 🔴 行情接口以当前时刻为结束日（包含未发生的 bar）

`昨日炸板股策略.py:166` · **HIGH** · `QUANT-FF-GETPRICE-NOW`

JoinQuant 的 get_price/attribute_history 在 end_date=context.current_dt 时会把当根 bar 也算入，若据此下单即为未来函数。

**证据**

```text
df = get_price(stock, start_date=g.run_time, end_date=context.current_dt, frequency='1m', fields=['close','high', 'high_limit'], panel=False)
```

**证据引用：** `local-rule:fab6e66d184e2927`, `diff-ast:2d8171c2e5b39169`

**修复建议：** 用 context.previous_date 作为结束日，或改用 history()（默认不含当根）。

**测试建议：** 断言行情查询的结束日早于当前信号 bar。

### 7. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`昨日炸板股策略.py:52` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
securities = get_index_stocks('000001.XSHG')+get_index_stocks('399001.XSHE')
```

**证据引用：** `local-rule:9b8a83b7827f459f`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 8. 🟠 裸异常吞掉错误

`昨日炸板股策略.py:190` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:869ae24d159bdc5e`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

