# EvoAgent 量化代码审查

**Repository:** `聚宽2021/多因子选股`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 23 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `16`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `225 ms`

## 审查发现

### 1. 🔴 策略逻辑使用了真实当前时间

`多因子选股.py:195` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
current_date = datetime.datetime.now()
```

**证据引用：** `local-rule:335ce2d12d4a4078`, `diff-ast:0df4123b0afbfa38`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股.py:430` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price('000001.XSHG', end_date=end, count=count)
```

**证据引用：** `local-rule:08c83df180aaafd2`, `diff-ast:ecd3a00c56755df0`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股.py:432` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price('000001.XSHG', start_date=start, end_date=end)
```

**证据引用：** `local-rule:b3612d2b664067fa`, `diff-ast:9a6368cada6b9391`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股.py:797` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
money = get_price(stock,end_date=pubdate_stock,count=past_days,fields=['money'])['money']
```

**证据引用：** `local-rule:932150bb5a968fcb`, `diff-ast:b03687a3fe877df4`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股.py:806` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
money = get_price(stocks,end_date=date,count=past_days,fields=['money'])['money']
```

**证据引用：** `local-rule:e6d1f1bbf1efd386`, `diff-ast:e689e173a8846d97`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 6. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股.py:834` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
money = get_price(stock,end_date=pubdate_stock,count=past_days,fields=['volume'])['volume']
```

**证据引用：** `local-rule:607b90a2a0b29ae5`, `diff-ast:426c916538472f83`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 7. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股.py:843` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
money = get_price(stocks,end_date=date,count=past_days,fields=['volume'])['volume']
```

**证据引用：** `local-rule:e049324b3c02fc51`, `diff-ast:7ab9efb1882a5360`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 8. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股.py:916` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
end_price = get_price(stocks,end_date=date,count=1,fields=['close'])['close']
```

**证据引用：** `local-rule:7eb36ae6d586c170`, `diff-ast:42a6c88f33d14f2d`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 9. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股.py:918` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
start_price = get_price(stocks,end_date=start_date,count=1,fields=['close'])['close']
```

**证据引用：** `local-rule:e2f923ffa377dd53`, `diff-ast:144e20f2de9d4f1f`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 10. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股.py:1074` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
start_price = get_price(stocks,end_date=start_date,count=1,fields=['close'])['close']
```

**证据引用：** `local-rule:5c38c97b5bdf6f11`, `diff-ast:264a2db7595faa88`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 11. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股.py:1077` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
end_price = get_price(stocks,end_date=end_date,count=1,fields=['close'])['close']
```

**证据引用：** `local-rule:a4952a7823ab9c55`, `diff-ast:705920810c385107`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 12. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`多因子选股.py:1086` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
end_price = get_price(stocks,end_date=end_date,count=1,fields=['close'])['close']
```

**证据引用：** `local-rule:293afa06f0fe0d46`, `diff-ast:181e9ed5589a70e3`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 13. 🟠 使用 statDate 拉取财报期数据，可能包含未披露数据

`多因子选股.py:360` · **MEDIUM** · `QUANT-LK-STATDATE`

用 statDate 指定报告期会在报告实际发布前取到数据，形成前视。

**证据**

```text
fundamental_data = get_fundamentals(ttm_q, statDate=statdate)
```

**证据引用：** `local-rule:b3a9ea5fd7e85fe5`

**修复建议：** 用 point-in-time 的发布日字段，或在回测日之前取最近已披露财报。

**测试建议：** 断言取到的财报披露日不晚于回测当前日。

### 14. 🟠 使用 statDate 拉取财报期数据，可能包含未披露数据

`多因子选股.py:364` · **MEDIUM** · `QUANT-LK-STATDATE`

用 statDate 指定报告期会在报告实际发布前取到数据，形成前视。

**证据**

```text
fundamental_data = get_fundamentals(ttm_q, statDate=statdate)
```

**证据引用：** `local-rule:8dc588ffd3da87d9`

**修复建议：** 用 point-in-time 的发布日字段，或在回测日之前取最近已披露财报。

**测试建议：** 断言取到的财报披露日不晚于回测当前日。

### 15. 🟠 使用 statDate 拉取财报期数据，可能包含未披露数据

`多因子选股.py:393` · **MEDIUM** · `QUANT-LK-STATDATE`

用 statDate 指定报告期会在报告实际发布前取到数据，形成前视。

**证据**

```text
fund_data = get_fundamentals(q, statDate=date)
```

**证据引用：** `local-rule:790e9320abe29736`

**修复建议：** 用 point-in-time 的发布日字段，或在回测日之前取最近已披露财报。

**测试建议：** 断言取到的财报披露日不晚于回测当前日。

### 16. 🟠 使用 statDate 拉取财报期数据，可能包含未披露数据

`多因子选股.py:490` · **MEDIUM** · `QUANT-LK-STATDATE`

用 statDate 指定报告期会在报告实际发布前取到数据，形成前视。

**证据**

```text
fund_data = get_fundamentals(q, statDate=date)
```

**证据引用：** `local-rule:d683be60ff738921`

**修复建议：** 用 point-in-time 的发布日字段，或在回测日之前取最近已披露财报。

**测试建议：** 断言取到的财报披露日不晚于回测当前日。

### 17. 🟠 使用 statDate 拉取财报期数据，可能包含未披露数据

`多因子选股.py:501` · **MEDIUM** · `QUANT-LK-STATDATE`

用 statDate 指定报告期会在报告实际发布前取到数据，形成前视。

**证据**

```text
fund_data = get_fundamentals(q, statDate=date)
```

**证据引用：** `local-rule:5bba66d567cf23fa`

**修复建议：** 用 point-in-time 的发布日字段，或在回测日之前取最近已披露财报。

**测试建议：** 断言取到的财报披露日不晚于回测当前日。

### 18. 🟠 裸异常吞掉错误

`多因子选股.py:674` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:3b1f487c7c0b3c07`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 19. 🟠 裸异常吞掉错误

`多因子选股.py:1125` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:a73e1004d87a70b9`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 20. 🟡 用 0 填补缺失值可能扭曲收益率

`多因子选股.py:575` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
df = df.fillna(0)
```

**证据引用：** `local-rule:c75fd1359f3a2f4c`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 21. 🟡 用 0 填补缺失值可能扭曲收益率

`多因子选股.py:891` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
res = pd.concat(cap_increase_l).fillna(0) #部分股票没有去年数据，用0填充
```

**证据引用：** `local-rule:62a9412cb12ff7e1`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 22. 🟡 非交互式回测中调用 plt.show()

`多因子选股.py:1246` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:多因子选股.py:1246`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 23. 🟡 非交互式回测中调用 plt.show()

`多因子选股.py:1433` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:多因子选股.py:1433`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

