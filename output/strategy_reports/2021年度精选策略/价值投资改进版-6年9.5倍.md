# EvoAgent 量化代码审查

**Repository:** `聚宽2021/价值投资改进版-6年9.5倍`  
**Risk:** `medium`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 2 个可处理问题。整体风险：medium。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `4`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `155 ms`

## 审查发现

### 1. 🟠 使用 statDate 拉取财报期数据，可能包含未披露数据

`价值投资改进版-6年9.5倍.py:154` · **MEDIUM** · `QUANT-LK-STATDATE`

用 statDate 指定报告期会在报告实际发布前取到数据，形成前视。

**证据**

```text
df = get_fundamentals(query(cash_flow.code,cash_flow.statDate,cash_flow.net_operate_cash_flow , \
```

**证据引用：** `local-rule:e0254eacd9d08fc7`

**修复建议：** 用 point-in-time 的发布日字段，或在回测日之前取最近已披露财报。

**测试建议：** 断言取到的财报披露日不晚于回测当前日。

### 2. 🟠 使用 statDate 拉取财报期数据，可能包含未披露数据

`价值投资改进版-6年9.5倍.py:227` · **MEDIUM** · `QUANT-LK-STATDATE`

用 statDate 指定报告期会在报告实际发布前取到数据，形成前视。

**证据**

```text
pre_panel = { quarter:get_fundamentals(nq, statDate = quarter) for quarter in quarters }
```

**证据引用：** `local-rule:d5150d4d1509796d`

**修复建议：** 用 point-in-time 的发布日字段，或在回测日之前取最近已披露财报。

**测试建议：** 断言取到的财报披露日不晚于回测当前日。

