# EvoAgent 量化代码审查

**Repository:** `聚宽2021/超稳+翻倍，贝塔值只有0.048的期指策略`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 8 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `13`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `164 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`超稳+翻倍，贝塔值只有0.048的期指策略.py:176` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
call_money = get_price(call,end_date=today ,fields='money',count=1,panel=False)
```

**证据引用：** `local-rule:29798f43ccdea34d`, `diff-ast:4132dbac7f26f110`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`超稳+翻倍，贝塔值只有0.048的期指策略.py:177` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
call_volume = get_price(call,end_date=today ,fields='volume',count=1,panel=False)
```

**证据引用：** `local-rule:c2409d7c44f0e5f9`, `diff-ast:a4a08e8e1c4934a2`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`超稳+翻倍，贝塔值只有0.048的期指策略.py:178` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
put_money = get_price(put,end_date=today,fields='money',count=1,panel=False)
```

**证据引用：** `local-rule:fc42d99714ded9af`, `diff-ast:a64800e5bddadf68`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`超稳+翻倍，贝塔值只有0.048的期指策略.py:179` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
put_volume = get_price(put,end_date=today,fields='volume',count=1,panel=False)
```

**证据引用：** `local-rule:88da2e7786c560d1`, `diff-ast:42e04950cbbe829e`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`超稳+翻倍，贝塔值只有0.048的期指策略.py:184` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
etf_return = get_price(g.code,end_date=today,fields='close',count=21,panel=False).close.pct_change().dropna().tolist()
```

**证据引用：** `local-rule:48f74893e2f2d0a6`, `diff-ast:99c2b58dfd44b59f`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 6. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`超稳+翻倍，贝塔值只有0.048的期指策略.py:203` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
szcz = get_price('399001.XSHE',end_date=today,fields='money',count=10,panel=False)/1e8
```

**证据引用：** `local-rule:dd9bf1223102d61f`, `diff-ast:f0b0df844023bf3d`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 7. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`超稳+翻倍，贝塔值只有0.048的期指策略.py:204` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
szzz = get_price('000001.XSHG',end_date=today,fields='money',count=10,panel=False)/1e8
```

**证据引用：** `local-rule:f90671bbd7c7e478`, `diff-ast:b668cc2141552afb`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 8. 🟠 裸异常吞掉错误

`超稳+翻倍，贝塔值只有0.048的期指策略.py:199` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:8e9f8cfec2ae6635`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

