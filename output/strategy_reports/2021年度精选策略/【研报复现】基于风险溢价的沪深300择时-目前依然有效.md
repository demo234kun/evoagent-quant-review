# EvoAgent 量化代码审查

**Repository:** `聚宽2021/【研报复现】基于风险溢价的沪深300择时-目前依然有效`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 5 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `7`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `242 ms`

## 审查发现

### 1. 🚨 回测使用未来数据 (shift(-N))

`【研报复现】基于风险溢价的沪深300择时-目前依然有效.py:244` · **CRITICAL** · `QUANT-FF-SHIFT-NEG`

用 df.shift(-1) 等把下一根 bar 的数据对齐到当前行，会让信号偷看到未发生的价格，回测收益虚高。

**证据**

```text
pre_mark = np.nan_to_num(df["positon"].shift(-1))
```

**证据引用：** `local-rule:1c0cd82af0f2e6e1`, `diff-ast:7bd735b961042dca`

**修复建议：** 需要用到未来信息时改为 shift(正数) 或显式构造下一根标签；因子与信号必须只用当前及之前的数据。

**测试建议：** 构造一个已知结果的DataFrame，断言信号行不会引用其后的价格。

### 2. 🔴 策略逻辑使用了真实当前时间

`【研报复现】基于风险溢价的沪深300择时-目前依然有效.py:69` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
end_date = dt.datetime.now()
```

**证据引用：** `local-rule:b16a0035d918db80`, `diff-ast:3b6122f764dc0f08`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`【研报复现】基于风险溢价的沪深300择时-目前依然有效.py:81` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
price = get_price('399300.XSHE', count = 2000, end_date=end_date, frequency='daily', fields=['open', 'close'])
```

**证据引用：** `local-rule:189813994448eb3c`, `diff-ast:6ea7c3e02231ed21`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🟡 非交互式回测中调用 plt.show()

`【研报复现】基于风险溢价的沪深300择时-目前依然有效.py:190` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:【研报复现】基于风险溢价的沪深300择时-目前依然有效.py:190`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 5. 🟡 非交互式回测中调用 plt.show()

`【研报复现】基于风险溢价的沪深300择时-目前依然有效.py:209` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:【研报复现】基于风险溢价的沪深300择时-目前依然有效.py:209`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

