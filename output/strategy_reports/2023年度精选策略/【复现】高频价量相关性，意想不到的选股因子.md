# EvoAgent 量化代码审查

**Repository:** `聚宽2023/【复现】高频价量相关性，意想不到的选股因子`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 3 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `5`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `175 ms`

## 审查发现

### 1. 🔴 在全样本上 fit_transform 造成数据泄露

`【复现】高频价量相关性，意想不到的选股因子.py:89` · **HIGH** · `QUANT-LK-FULL-NORM`

在训练/测试切分之前对整个数据集 fit_transform，会让测试集的统计信息泄漏进训练。

**证据**

```text
pca_v = pca.fit_transform(v)
```

**证据引用：** `local-rule:6f490a4d62691a65`, `diff-ast:b5113f70a16c3e5f`

**修复建议：** 先切分再分别在训练集 fit，并用同一 scaler 仅 transform 测试集。

**测试建议：** 对比全样本缩放与时序切分缩放下的测试集指标，断言两者不同且后者更保守。

### 2. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`【复现】高频价量相关性，意想不到的选股因子.py:107` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
cons_symbol = get_index_stocks(g.is_in_index)
```

**证据引用：** `local-rule:b514dc309f487259`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 3. 🟡 用 0 填补缺失值可能扭曲收益率

`【复现】高频价量相关性，意想不到的选股因子.py:87` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
v = g.factor_df.loc[trade_date].fillna(0).values
```

**证据引用：** `local-rule:f8e21410ba4e909a`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

