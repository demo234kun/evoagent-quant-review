# EvoAgent 量化代码审查

**Repository:** `聚宽2023/【分享】券商金股组合增强`  
**Risk:** `low`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 3 个可处理问题。整体风险：low。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `4`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `192 ms`

## 审查发现

### 1. 🟡 用 0 填补缺失值可能扭曲收益率

`【分享】券商金股组合增强.py:500` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
z_score = (self.past_factor.fillna(0) -
```

**证据引用：** `local-rule:eb4f060023fc7c48`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 2. 🟡 用 0 填补缺失值可能扭曲收益率

`【分享】券商金股组合增强.py:541` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
return df.fillna(0).groupby(level='date').apply(scr_ic)
```

**证据引用：** `local-rule:334f65f20153b6d9`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 3. 🟡 用 0 填补缺失值可能扭曲收益率

`【分享】券商金股组合增强.py:568` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
df = pd.concat((factor, next_returns), axis=1).fillna(0)
```

**证据引用：** `local-rule:7e0005eff1cf415e`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

