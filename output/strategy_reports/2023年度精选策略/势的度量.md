# EvoAgent 量化代码审查

**Repository:** `聚宽2023/势的度量`  
**Risk:** `low`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 3 个可处理问题。整体风险：low。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `4`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `207 ms`

## 审查发现

### 1. 🟡 用 0 填补缺失值可能扭曲收益率

`势的度量.py:26` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
std_closes = sign.cumsum().fillna(0).values
```

**证据引用：** `local-rule:0b83cf090d2a9dea`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 2. 🟡 用 0 填补缺失值可能扭曲收益率

`势的度量.py:39` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
std_closes = sign.cumsum().fillna(0).values
```

**证据引用：** `local-rule:ad8aab2c1b5696ee`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 3. 🟡 用 0 填补缺失值可能扭曲收益率

`势的度量.py:61` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
std_closes = sign_compound.cumsum().fillna(0).values
```

**证据引用：** `local-rule:2e57825ee3d4cf85`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

