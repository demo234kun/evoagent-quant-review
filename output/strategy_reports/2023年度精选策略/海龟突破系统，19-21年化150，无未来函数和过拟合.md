# EvoAgent 量化代码审查

**Repository:** `聚宽2023/海龟突破系统，19-21年化150，无未来函数和过拟合`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 2 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `5`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `171 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`海龟突破系统，19-21年化150，无未来函数和过拟合.py:81` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
up_today_p=get_price(stock, start_date=up_today, end_date=up_today, frequency='daily', fields=None, skip_paused=False, fq='pre', panel=True, fill_paused=True)['close'][0]
```

**证据引用：** `local-rule:6cb50505dbea61dc`, `diff-ast:e6dfe3ccc9e1d6f6`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`海龟突破系统，19-21年化150，无未来函数和过拟合.py:20` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
g.security=get_index_stocks('000300.XSHG')
```

**证据引用：** `local-rule:bc3c4245e00ebf93`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

