# EvoAgent 量化代码审查

**Repository:** `聚宽2020/中长线买入卖出点选择`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 5 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `7`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `173 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`中长线买入卖出点选择.py:343` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
prices = get_price(stock, start_date=start, end_date = end, frequency='daily', skip_paused=True)
```

**证据引用：** `local-rule:e256401344d09267`, `diff-ast:3906d6af28e75067`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 行情接口以当前时刻为结束日（包含未发生的 bar）

`中长线买入卖出点选择.py:374` · **HIGH** · `QUANT-FF-GETPRICE-NOW`

JoinQuant 的 get_price/attribute_history 在 end_date=context.current_dt 时会把当根 bar 也算入，若据此下单即为未来函数。

**证据**

```text
prices = get_price(stock, count=M, end_date=context.current_dt, frequency='1d', fields=['high', 'low'], skip_paused=True)
```

**证据引用：** `local-rule:bc0b4e31a3051331`, `diff-ast:f1830d46bfbdaf33`

**修复建议：** 用 context.previous_date 作为结束日，或改用 history()（默认不含当根）。

**测试建议：** 断言行情查询的结束日早于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`中长线买入卖出点选择.py:432` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
stock_m = get_price(stock, count=n+n2+1, end_date=today, frequency='daily', fields=['money'], skip_paused=True)
```

**证据引用：** `local-rule:c8d797f301a2c572`, `diff-ast:3cdb5c64d3d20e8f`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`中长线买入卖出点选择.py:61` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
option_list = get_index_stocks('000300.XSHG')   # 沪深300成分股
```

**证据引用：** `local-rule:8c575da734d578f7`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 5. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`中长线买入卖出点选择.py:485` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
bank_stocks = get_index_stocks('399951.XSHE')
```

**证据引用：** `local-rule:bd5b37144585cd4c`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

