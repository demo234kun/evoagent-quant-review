# EvoAgent 量化代码审查

**Repository:** `聚宽2020/为什么股票市值是当前的样子？`  
**Risk:** `low`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 3 个可处理问题。整体风险：low。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `4`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `172 ms`

## 审查发现

### 1. 🟡 用 0 填补缺失值可能扭曲收益率

`为什么股票市值是当前的样子？.py:69` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
df = df.fillna(0)
```

**证据引用：** `local-rule:892406f9850d5713`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 2. 🟡 用 0 填补缺失值可能扭曲收益率

`为什么股票市值是当前的样子？.py:87` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
X = X.fillna(0)
```

**证据引用：** `local-rule:992f01b2bf57ffb0`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 3. 🟡 用 0 填补缺失值可能扭曲收益率

`为什么股票市值是当前的样子？.py:88` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
Y = Y.fillna(0)
```

**证据引用：** `local-rule:5da4a55648e65a02`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

