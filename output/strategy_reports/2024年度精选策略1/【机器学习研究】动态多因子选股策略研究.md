# EvoAgent 量化代码审查

**Repository:** `聚宽2024/【机器学习研究】动态多因子选股策略研究`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 8 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `6`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `201 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`【机器学习研究】动态多因子选股策略研究.py:61` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
hs300_close = get_price(ref_index_stock, five_days_ago, today, fq='pre')['close']
```

**证据引用：** `local-rule:0a3154b28fd3ec9c`, `diff-ast:cd2a589e095de0ef`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 在全样本上 fit_transform 造成数据泄露

`【机器学习研究】动态多因子选股策略研究.py:352` · **HIGH** · `QUANT-LK-FULL-NORM`

在训练/测试切分之前对整个数据集 fit_transform，会让测试集的统计信息泄漏进训练。

**证据**

```text
x_new_train=scaler.fit_transform(x_new_train)
```

**证据引用：** `local-rule:a541a662c9fe7531`, `diff-ast:defaf80a4136dfb5`

**修复建议：** 先切分再分别在训练集 fit，并用同一 scaler 仅 transform 测试集。

**测试建议：** 对比全样本缩放与时序切分缩放下的测试集指标，断言两者不同且后者更保守。

### 3. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`【机器学习研究】动态多因子选股策略研究.py:99` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
hs300_stock_lst = get_index_stocks('000300.XSHG')
```

**证据引用：** `local-rule:25242fd98c554847`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 4. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`【机器学习研究】动态多因子选股策略研究.py:523` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stocks = get_index_stocks('000300.XSHG')
```

**证据引用：** `local-rule:17369a5f837dac7a`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 5. 🟡 用 0 填补缺失值可能扭曲收益率

`【机器学习研究】动态多因子选股策略研究.py:246` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
x = x.fillna(0)
```

**证据引用：** `local-rule:d09d3ed458a56f3f`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 6. 🟡 用 0 填补缺失值可能扭曲收益率

`【机器学习研究】动态多因子选股策略研究.py:247` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
y = y.fillna(0)
```

**证据引用：** `local-rule:b7f2f4797a7b5009`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 7. 🟡 用 0 填补缺失值可能扭曲收益率

`【机器学习研究】动态多因子选股策略研究.py:343` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
x_new = x_new.fillna(0)
```

**证据引用：** `local-rule:ec4c8b5a9521a8db`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 8. 🟡 用 0 填补缺失值可能扭曲收益率

`【机器学习研究】动态多因子选股策略研究.py:344` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
y_new = y_new.fillna(0)
```

**证据引用：** `local-rule:8e71557e7dbe7386`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

