# EvoAgent 量化代码审查

**Repository:** `聚宽2024/业绩预告小工具--已更新`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 4 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `7`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `157 ms`

## 审查发现

### 1. 🔴 策略逻辑使用了真实当前时间

`业绩预告小工具--已更新.py:82` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
year=datetime.datetime.now().year
```

**证据引用：** `local-rule:214202f5ad9ca9fa`, `diff-ast:9d6ac124f5747a91`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 2. 🟠 使用 statDate 拉取财报期数据，可能包含未披露数据

`业绩预告小工具--已更新.py:99` · **MEDIUM** · `QUANT-LK-STATDATE`

用 statDate 指定报告期会在报告实际发布前取到数据，形成前视。

**证据**

```text
rets = [get_fundamentals(q1, statDate=i) for i in period]
```

**证据引用：** `local-rule:8b9ec1cc8ece85c8`

**修复建议：** 用 point-in-time 的发布日字段，或在回测日之前取最近已披露财报。

**测试建议：** 断言取到的财报披露日不晚于回测当前日。

### 3. 🟠 裸异常吞掉错误

`业绩预告小工具--已更新.py:154` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:98b9816889ddadca`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 4. 🟡 非交互式回测中调用 plt.show()

`业绩预告小工具--已更新.py:140` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
return plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:业绩预告小工具--已更新.py:140`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

