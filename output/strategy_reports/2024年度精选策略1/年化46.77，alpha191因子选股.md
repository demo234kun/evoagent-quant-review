# EvoAgent 量化代码审查

**Repository:** `聚宽2024/年化46.77，alpha191因子选股`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 7 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `7`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `169 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`年化46.77，alpha191因子选股.py:97` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price(security, end_date=end_date, count=period, panel=False, fill_paused=False)
```

**证据引用：** `local-rule:532bb8e7ed88b634`, `diff-ast:68074931f89f962c`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 策略代码中出现 eval/exec

`年化46.77，alpha191因子选股.py:160` · **HIGH** · `QUANT-CQ-UNSAFE-EVAL`

在量化脚本里执行动态代码存在注入与不可复现风险，且难以做静态审查。

**证据**

```text
df[factor] = np.array(eval(factor)(select_list, end_date=None))
```

**证据引用：** `scanner:QUANT-CQ-UNSAFE-EVAL:年化46.77，alpha191因子选股.py:160`, `diff-ast:134a161e2e1ff485`

**修复建议：** 改为显式函数与配置驱动的参数解析。

**测试建议：** 验证移除 eval/exec 后策略逻辑等价且可静态审查。

### 3. 🟠 set_order_cost 佣金/税费设为 0，回测收益虚高

`年化46.77，alpha191因子选股.py:27` · **MEDIUM** · `QUANT-EX-ZERO-ORDERCOST`

聚宽 set_order_cost 将佣金或税费设为 0 会高估策略真实收益，实盘存在成本。

**证据**

```text
set_order_cost(OrderCost(open_tax=0, close_tax=0.001, open_commission=0.0003, \
```

**证据引用：** `local-rule:d4bdaa2c7a1ae879`

**修复建议：** 按真实券商费率设置 open_commission/close_commission/close_tax。

**测试建议：** 对比零成本与真实成本下的收益与回撤差异。

### 4. 🟠 假设零手续费（set_commission 缺失/为 0）

`年化46.77，alpha191因子选股.py:28` · **MEDIUM** · `QUANT-EX-ZERO-COMM`

手续费为 0 会高估高频/换手策略收益。

**证据**

```text
close_commission=0.0003, close_today_commission=0, min_commission=5), type='stock')
```

**证据引用：** `local-rule:ade7802aea52146f`

**修复建议：** 设置与券商一致的佣金、印花税与最低手续费。

**测试建议：** 断言手续费计入后收益曲线符合预期。

### 5. 🟡 用 0 填补缺失值可能扭曲收益率

`年化46.77，alpha191因子选股.py:189` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
df = df.fillna(0)
```

**证据引用：** `local-rule:0156d35c8377108a`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 6. 🟡 用 0 填补缺失值可能扭曲收益率

`年化46.77，alpha191因子选股.py:195` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
X = X.fillna(0)
```

**证据引用：** `local-rule:44c9d5123d81bab0`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 7. 🟡 用 0 填补缺失值可能扭曲收益率

`年化46.77，alpha191因子选股.py:196` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
Y = Y.fillna(0)
```

**证据引用：** `local-rule:9a64fd1ffcc01700`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

