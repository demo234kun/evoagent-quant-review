# EvoAgent 量化代码审查

**Repository:** `聚宽2024/wywy1995大神机器学习策略年化提升35pt`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 4 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `7`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `173 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`wywy1995大神机器学习策略年化提升35pt.py:58` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price(g.hold_list, end_date=context.previous_date, frequency='daily', fields=['close','high_limit'], count=1, panel=False, fill_paused=False)
```

**证据引用：** `local-rule:7dfd09e3fabb0789`, `diff-ast:39ae93bb3cdca1d8`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`wywy1995大神机器学习策略年化提升35pt.py:137` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
current_data = get_price(stock, end_date=now_time, frequency='1m', fields=['close','high_limit'], skip_paused=False, fq='pre', count=1, panel=False, fill_paused=True)
```

**证据引用：** `local-rule:b96bc50811a553c4`, `diff-ast:4a91ef8e66732930`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 疑似硬编码交易凭据

`wywy1995大神机器学习策略年化提升35pt.py:287` · **HIGH** · `QUANT-SEC-HARDCODED-KEY`

交易账号、API Key、token 进入代码仓库后会通过历史记录与构建日志泄露。

**证据**

```text
password = '54e7a4' #你的邮箱密码 或 口令
```

**证据引用：** `local-rule:40702fa0f465ca3a`, `diff-ast:b758cb3203a2bfa0`

**修复建议：** 改为从密钥管理或环境变量读取，并立即轮换已提交的凭据。

**测试建议：** 测试缺失配置时安全失败，且日志不会输出凭据。

### 4. 🟠 假设零手续费（set_commission 缺失/为 0）

`wywy1995大神机器学习策略年化提升35pt.py:25` · **MEDIUM** · `QUANT-EX-ZERO-COMM`

手续费为 0 会高估高频/换手策略收益。

**证据**

```text
set_order_cost(OrderCost(open_tax=0, close_tax=0.001, open_commission=0.0003, close_commission=0.0003, close_today_commission=0, min_commission=5),type='stock')
```

**证据引用：** `local-rule:50f18bcf13143d11`

**修复建议：** 设置与券商一致的佣金、印花税与最低手续费。

**测试建议：** 断言手续费计入后收益曲线符合预期。

