# EvoAgent 量化代码审查

**Repository:** `聚宽2024/方正证券-多因子选股系列研究之四：“球队硬币”`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 3 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `5`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `175 ms`

## 审查发现

### 1. 🚨 回测使用未来数据 (shift(-N))

`方正证券-多因子选股系列研究之四：“球队硬币”.py:195` · **CRITICAL** · `QUANT-FF-SHIFT-NEG`

用 df.shift(-1) 等把下一根 bar 的数据对齐到当前行，会让信号偷看到未发生的价格，回测收益虚高。

**证据**

```text
df["ret_20_after"] = df.groupby("code")["ret_20"].shift(-20)
```

**证据引用：** `local-rule:89554c40aed09254`, `diff-ast:4ab0e9a92d4f21df`

**修复建议：** 需要用到未来信息时改为 shift(正数) 或显式构造下一根标签；因子与信号必须只用当前及之前的数据。

**测试建议：** 构造一个已知结果的DataFrame，断言信号行不会引用其后的价格。

### 2. 🟡 非交互式回测中调用 plt.show()

`方正证券-多因子选股系列研究之四：“球队硬币”.py:124` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:方正证券-多因子选股系列研究之四：“球队硬币”.py:124`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 3. 🟡 非交互式回测中调用 plt.show()

`方正证券-多因子选股系列研究之四：“球队硬币”.py:176` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:方正证券-多因子选股系列研究之四：“球队硬币”.py:176`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

