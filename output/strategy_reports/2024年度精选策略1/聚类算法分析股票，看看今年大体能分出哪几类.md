# EvoAgent 量化代码审查

**Repository:** `聚宽2024/聚类算法分析股票，看看今年大体能分出哪几类`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 11 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `6`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `184 ms`

## 审查发现

### 1. 🔴 策略逻辑使用了真实当前时间

`聚类算法分析股票，看看今年大体能分出哪几类.py:30` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
current_date = datetime.datetime.now()
```

**证据引用：** `local-rule:4c75f45132304a34`, `diff-ast:da718e7106ed2a26`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 2. 🔴 在全样本上 fit_transform 造成数据泄露

`聚类算法分析股票，看看今年大体能分出哪几类.py:113` · **HIGH** · `QUANT-LK-FULL-NORM`

在训练/测试切分之前对整个数据集 fit_transform，会让测试集的统计信息泄漏进训练。

**证据**

```text
embedded_data = tsne.fit_transform(DATA)
```

**证据引用：** `local-rule:5d245601bc35778e`, `diff-ast:62428d185b581c21`

**修复建议：** 先切分再分别在训练集 fit，并用同一 scaler 仅 transform 测试集。

**测试建议：** 对比全样本缩放与时序切分缩放下的测试集指标，断言两者不同且后者更保守。

### 3. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`聚类算法分析股票，看看今年大体能分出哪几类.py:45` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('000300.XSHG',begin_date)
```

**证据引用：** `local-rule:286004e48248a078`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 4. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`聚类算法分析股票，看看今年大体能分出哪几类.py:47` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('399905.XSHE',begin_date)
```

**证据引用：** `local-rule:e921e999800fba35`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 5. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`聚类算法分析股票，看看今年大体能分出哪几类.py:49` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('399906.XSHE',begin_date)
```

**证据引用：** `local-rule:b35bae02c334636a`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 6. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`聚类算法分析股票，看看今年大体能分出哪几类.py:51` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('399006.XSHE',begin_date)
```

**证据引用：** `local-rule:c67db203760e1e75`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 7. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`聚类算法分析股票，看看今年大体能分出哪几类.py:53` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('399005.XSHE',begin_date)
```

**证据引用：** `local-rule:f96a92ac405f4d71`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 8. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`聚类算法分析股票，看看今年大体能分出哪几类.py:55` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('000002.XSHG',begin_date)+get_index_stocks('399107.XSHE',begin_date)
```

**证据引用：** `local-rule:26232b7ca7cbb599`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 9. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`聚类算法分析股票，看看今年大体能分出哪几类.py:58` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('000985.XSHG',begin_date)
```

**证据引用：** `local-rule:a58326ad7bd3c69a`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 10. 🟡 非交互式回测中调用 plt.show()

`聚类算法分析股票，看看今年大体能分出哪几类.py:119` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:聚类算法分析股票，看看今年大体能分出哪几类.py:119`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 11. 🟡 非交互式回测中调用 plt.show()

`聚类算法分析股票，看看今年大体能分出哪几类.py:167` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:聚类算法分析股票，看看今年大体能分出哪几类.py:167`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

