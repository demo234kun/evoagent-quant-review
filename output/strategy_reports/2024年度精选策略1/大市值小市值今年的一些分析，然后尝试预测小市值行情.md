# EvoAgent 量化代码审查

**Repository:** `聚宽2024/大市值小市值今年的一些分析，然后尝试预测小市值行情`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 15 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `7`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `203 ms`

## 审查发现

### 1. 🔴 策略逻辑使用了真实当前时间

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:33` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
current_date = datetime.datetime.now()
```

**证据引用：** `local-rule:0c15a4694b27e1da`, `diff-ast:d67c9559561138f2`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 2. 🔴 策略逻辑使用了真实当前时间

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:261` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
today = datetime.datetime.today()
```

**证据引用：** `local-rule:58df6241dc4dc576`, `diff-ast:efc2f14c2ee7bb93`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 3. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:48` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('000300.XSHG',begin_date)
```

**证据引用：** `local-rule:038997c0d8c60fa3`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 4. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:50` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('399905.XSHE',begin_date)
```

**证据引用：** `local-rule:e69da622ac2090aa`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 5. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:52` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('399906.XSHE',begin_date)
```

**证据引用：** `local-rule:5d0c004c5323081f`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 6. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:54` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('399006.XSHE',begin_date)
```

**证据引用：** `local-rule:3cf64b45d989fb69`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 7. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:56` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('399005.XSHE',begin_date)
```

**证据引用：** `local-rule:5cef45dd777cad4f`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 8. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:58` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('000002.XSHG',begin_date)+get_index_stocks('399107.XSHE',begin_date)
```

**证据引用：** `local-rule:90be8b9a6dd573cd`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 9. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:61` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('000985.XSHG',begin_date)
```

**证据引用：** `local-rule:4447416fca48fda1`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 10. 🟡 非交互式回测中调用 plt.show()

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:138` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:大市值小市值今年的一些分析，然后尝试预测小市值行情.py:138`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 11. 🟡 非交互式回测中调用 plt.show()

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:184` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:大市值小市值今年的一些分析，然后尝试预测小市值行情.py:184`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 12. 🟡 非交互式回测中调用 plt.show()

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:204` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
# plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:大市值小市值今年的一些分析，然后尝试预测小市值行情.py:204`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 13. 🟡 非交互式回测中调用 plt.show()

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:225` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:大市值小市值今年的一些分析，然后尝试预测小市值行情.py:225`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 14. 🟡 非交互式回测中调用 plt.show()

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:246` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:大市值小市值今年的一些分析，然后尝试预测小市值行情.py:246`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 15. 🟡 非交互式回测中调用 plt.show()

`大市值小市值今年的一些分析，然后尝试预测小市值行情.py:423` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:大市值小市值今年的一些分析，然后尝试预测小市值行情.py:423`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

