# EvoAgent 量化代码审查

**Repository:** `聚宽2024/实盘模拟的小盘股策略，低频，带基本面优化以及个股风控`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 4 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `5`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `207 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`实盘模拟的小盘股策略，低频，带基本面优化以及个股风控.py:128` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_cost = get_price(stock, count=d_count.days + 2, end_date=buy_time_s, frequency='daily', fields=['high'])
```

**证据引用：** `local-rule:7d7ddea2b97f43d9`, `diff-ast:c5da94fae07027a0`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🟠 set_order_cost 佣金/税费设为 0，回测收益虚高

`实盘模拟的小盘股策略，低频，带基本面优化以及个股风控.py:26` · **MEDIUM** · `QUANT-EX-ZERO-ORDERCOST`

聚宽 set_order_cost 将佣金或税费设为 0 会高估策略真实收益，实盘存在成本。

**证据**

```text
set_order_cost(OrderCost(open_tax=0, close_tax=0.001, open_commission=0.0003, close_commission=0.0003, min_commission=5), type='stock')
```

**证据引用：** `local-rule:30dd799f9656caa0`

**修复建议：** 按真实券商费率设置 open_commission/close_commission/close_tax。

**测试建议：** 对比零成本与真实成本下的收益与回撤差异。

### 3. 🟠 裸异常吞掉错误

`实盘模拟的小盘股策略，低频，带基本面优化以及个股风控.py:319` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:15215f83bfb2f734`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 4. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`实盘模拟的小盘股策略，低频，带基本面优化以及个股风控.py:379` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
temp_index += get_index_stocks(s)
```

**证据引用：** `local-rule:6cb90ea03bcf2f3f`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

