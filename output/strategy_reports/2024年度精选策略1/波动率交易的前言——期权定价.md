# EvoAgent 量化代码审查

**Repository:** `聚宽2024/波动率交易的前言——期权定价`  
**Risk:** `medium`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 2 个可处理问题。整体风险：medium。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `4`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `193 ms`

## 审查发现

### 1. 🟠 裸异常吞掉错误

`波动率交易的前言——期权定价.py:95` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:f3b67228a350ad14`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 2. 🟡 用 0 填补缺失值可能扭曲收益率

`波动率交易的前言——期权定价.py:373` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
sub_mid = mid.loc[:,t][mid.loc[:,t].fillna(0) > mid.iloc[:,columns.index(t) - 1]]
```

**证据引用：** `local-rule:a68d174004205947`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

