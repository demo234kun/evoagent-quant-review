# EvoAgent 量化代码审查

**Repository:** `聚宽2024/全市场归因`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 15 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `18`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `194 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`全市场归因.py:48` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price('000001.XSHG',end_date=end,count=count)#有计数n，返回后n天
```

**证据引用：** `local-rule:0540acf38f128136`, `diff-ast:bf6ee654aa2989e4`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`全市场归因.py:50` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price('000001.XSHG',start_date=start,end_date=end)#否则返回始末日期之间
```

**证据引用：** `local-rule:24d001daa58bffa9`, `diff-ast:10fdfeb112203c5d`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`全市场归因.py:100` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
is_susp = get_price(stockList,end_date=date, count=1,fields='paused'                        ,panel=False).set_index('code')[['paused']]
```

**证据引用：** `local-rule:8275571895897b2c`, `diff-ast:93448162e21e41f2`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`全市场归因.py:109` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price(stockList,end_date=date,                       fields=['open','high_limit','low_limit'],count=1).iloc[:,0,:]
```

**证据引用：** `local-rule:f0c07d7c96b8d0fe`, `diff-ast:623b6661f6aa3794`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`全市场归因.py:342` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_1 = get_price(index,end_date=d1,fields=['open'],count = 1)['open']
```

**证据引用：** `local-rule:9867e417c56d1b13`, `diff-ast:3b74c071ac8b4744`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 6. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`全市场归因.py:344` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_2 = get_price(index,end_date=d2,fields=['open'],count = 1)['open']
```

**证据引用：** `local-rule:0bc2fc68bc0b61c3`, `diff-ast:b2e2cdd0aa3cfcfb`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 7. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`全市场归因.py:349` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_1 = get_price(pool,end_date=d1,fields=['open'],count = 1)['open']
```

**证据引用：** `local-rule:cc6d800b92d842da`, `diff-ast:a765bcaa0264586d`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 8. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`全市场归因.py:350` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_2 = get_price(pool,end_date=d2,fields=['open'],count = 1)['open']
```

**证据引用：** `local-rule:c47e70a36e280aa3`, `diff-ast:269b95b65ddb4404`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 9. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`全市场归因.py:683` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df1 = get_price(pool,end_date=t2,fields=['open'],count = 1)['open']
```

**证据引用：** `local-rule:776ff258c6a2fb07`, `diff-ast:f5961b6d99e73b56`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 10. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`全市场归因.py:684` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df2 = get_price(pool,end_date=t3,fields=['open'],count = 1)['open']
```

**证据引用：** `local-rule:035c132e10f7991d`, `diff-ast:dda2e10ea2c36dc5`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 11. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`全市场归因.py:812` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
price1 = get_price(group,end_date=datetimeindex,                           fields=['open'],count = 1)['open']
```

**证据引用：** `local-rule:5a1492d0e5c9ea4f`, `diff-ast:a94d0e53792353e7`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 12. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`全市场归因.py:813` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
price2 = get_price(group,end_date=list(date_list)[idx+2],                           fields=['open'],count = 1)['open']
```

**证据引用：** `local-rule:8d63e2d63115aa8f`, `diff-ast:e656a98bec658509`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 13. 🟡 用 0 填补缺失值可能扭曲收益率

`全市场归因.py:138` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
return df.fillna(0)
```

**证据引用：** `local-rule:0ec2de014357adca`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 14. 🟡 非交互式回测中调用 plt.show()

`全市场归因.py:659` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:全市场归因.py:659`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 15. 🟡 用 0 填补缺失值可能扭曲收益率

`全市场归因.py:703` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
temp_df = temp_df.fillna(0)
```

**证据引用：** `local-rule:bbda21bbf269890e`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

