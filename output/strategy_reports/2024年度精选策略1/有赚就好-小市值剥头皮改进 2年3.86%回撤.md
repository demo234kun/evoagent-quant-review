# EvoAgent 量化代码审查

**Repository:** `聚宽2024/有赚就好-小市值剥头皮改进 2年3.86%回撤`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 1 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `6`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `155 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`有赚就好-小市值剥头皮改进 2年3.86%回撤.py:103` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
prev_close = get_price(s, count=1, end_date=context.previous_date).iloc[-1]['close']
```

**证据引用：** `local-rule:fcd07988c66a39bc`, `diff-ast:ed5f885df3fe62af`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

