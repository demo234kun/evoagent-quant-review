# EvoAgent 量化代码审查

**Repository:** `聚宽2026/贡献给大家注意回测不可超过2年自动清理历史数据`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 18 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `9`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `249 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`贡献给大家注意回测不可超过2年自动清理历史数据.py:1471` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
sh_money = get_price('000001.XSHG', start_date=start_date, end_date=yesterday, fields='money')['money']
```

**证据引用：** `local-rule:23aeff4899f24684`, `diff-ast:f0c41dc36f7e2306`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`贡献给大家注意回测不可超过2年自动清理历史数据.py:1472` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
sz_money = get_price('399001.XSHE', start_date=start_date, end_date=yesterday, fields='money')['money']
```

**证据引用：** `local-rule:fb01fd9f4deb19aa`, `diff-ast:77eb107a7a8aa8f5`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`贡献给大家注意回测不可超过2年自动清理历史数据.py:1484` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
price_df = get_price('399986.XSHE', end_date=yesterday, count=ma_window, fields='close', skip_paused=True)
```

**证据引用：** `local-rule:a86c77d2f88d3373`, `diff-ast:1d83b5f3dbe7e3fc`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`贡献给大家注意回测不可超过2年自动清理历史数据.py:1940` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price(chunk_codes, count=1, end_date=end_date, frequency='daily', fields=['money'], panel=False)
```

**证据引用：** `local-rule:113ede55dde00767`, `diff-ast:58de6c138584c666`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🟠 set_order_cost 佣金/税费设为 0，回测收益虚高

`贡献给大家注意回测不可超过2年自动清理历史数据.py:48` · **MEDIUM** · `QUANT-EX-ZERO-ORDERCOST`

聚宽 set_order_cost 将佣金或税费设为 0 会高估策略真实收益，实盘存在成本。

**证据**

```text
set_order_cost(OrderCost(open_tax=0, close_tax=0, open_commission=0.0001, close_commission=0.0001, close_today_commission=0.0001, min_commission=5), type="fund")
```

**证据引用：** `local-rule:746acbdbed324ad9`

**修复建议：** 按真实券商费率设置 open_commission/close_commission/close_tax。

**测试建议：** 对比零成本与真实成本下的收益与回撤差异。

### 6. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`贡献给大家注意回测不可超过2年自动清理历史数据.py:725` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
all_stocks = get_index_stocks(strategy['config'].BENCHMARK)
```

**证据引用：** `local-rule:829f08921b8b6770`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 7. 🟠 裸异常吞掉错误

`贡献给大家注意回测不可超过2年自动清理历史数据.py:1274` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:9288e13e2b18f813`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 8. 🟠 裸异常吞掉错误

`贡献给大家注意回测不可超过2年自动清理历史数据.py:1354` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:cc8f38227f44cde5`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 9. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`贡献给大家注意回测不可超过2年自动清理历史数据.py:1508` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
initial_list = get_index_stocks('000985.XSHG', context.current_dt)
```

**证据引用：** `local-rule:c9cbed0d218b80c0`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 10. 🟠 裸异常吞掉错误

`贡献给大家注意回测不可超过2年自动清理历史数据.py:1583` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:4e133bcf066f1d46`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 11. 🟠 裸异常吞掉错误

`贡献给大家注意回测不可超过2年自动清理历史数据.py:1635` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:1111a5e0dc66d853`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 12. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`贡献给大家注意回测不可超过2年自动清理历史数据.py:1681` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stocks = get_index_stocks(index, dt_now)
```

**证据引用：** `local-rule:12f634bf268ab343`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 13. 🟠 裸异常吞掉错误

`贡献给大家注意回测不可超过2年自动清理历史数据.py:1777` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:956268071a56ebe9`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 14. 🟠 裸异常吞掉错误

`贡献给大家注意回测不可超过2年自动清理历史数据.py:1849` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:4a77967c32e28e3e`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 15. 🟠 裸异常吞掉错误

`贡献给大家注意回测不可超过2年自动清理历史数据.py:2031` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:4613c59a28eb1154`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 16. 🟠 裸异常吞掉错误

`贡献给大家注意回测不可超过2年自动清理历史数据.py:2051` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:24a894163161e72c`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 17. 🟠 裸异常吞掉错误

`贡献给大家注意回测不可超过2年自动清理历史数据.py:2141` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:fa50ef634845fcc7`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 18. 🟠 裸异常吞掉错误

`贡献给大家注意回测不可超过2年自动清理历史数据.py:2158` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:e88627ec30906dc0`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

