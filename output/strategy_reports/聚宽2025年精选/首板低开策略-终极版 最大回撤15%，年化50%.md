# EvoAgent 量化代码审查

**Repository:** `聚宽2025/首板低开策略-终极版 最大回撤15%，年化50%`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 9 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `13`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `242 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`首板低开策略-终极版 最大回撤15%，年化50%.py:90` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
stock_data = get_price(stock, end_date=yesterday, count=1, fields=['close', 'low', 'low_limit'])
```

**证据引用：** `local-rule:7aafffacfa14e594`, `diff-ast:8bb1f161d20a45e5`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`首板低开策略-终极版 最大回撤15%，年化50%.py:179` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
price_data = get_price(stock, end_date=date, count=2, frequency='daily', fields=['open', 'close'])
```

**证据引用：** `local-rule:104572c2aa847b7f`, `diff-ast:4f343aaee59004e9`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`首板低开策略-终极版 最大回撤15%，年化50%.py:204` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
stock_data = get_price(code, start_date=date, end_date=date, frequency='daily', fields=['open', 'pre_close'])
```

**证据引用：** `local-rule:363f7eb3a92490bb`, `diff-ast:f6e118f35bd703a4`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`首板低开策略-终极版 最大回撤15%，年化50%.py:254` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
data = get_price(code, end_date=date, count=watch_days, fields=['high', 'low'])
```

**证据引用：** `local-rule:3b7cdb8187e9abd0`, `diff-ast:f62e7b7f30afa8e4`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`首板低开策略-终极版 最大回撤15%，年化50%.py:258` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
close_price = get_price(code, end_date=date, count=1, fields=['close'])['close'].iloc[0]
```

**证据引用：** `local-rule:ad5ef59aa90db024`, `diff-ast:cc0a35cdfe353fe9`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 6. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`首板低开策略-终极版 最大回撤15%，年化50%.py:287` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
price_data = get_price(stock, end_date=date, count=2, frequency='daily', fields=['open', 'close'])
```

**证据引用：** `local-rule:a810862acdea3768`, `diff-ast:0cbc1c917fe1c7cc`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 7. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`首板低开策略-终极版 最大回撤15%，年化50%.py:312` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
stock_data = get_price(code, start_date=date, end_date=date, frequency='daily', fields=['open', 'pre_close'])
```

**证据引用：** `local-rule:047f9c4fe2ad63b6`, `diff-ast:5adeb30cc42958c7`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 8. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`首板低开策略-终极版 最大回撤15%，年化50%.py:358` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
data = get_price(code, end_date=date, count=watch_days, fields=['high', 'low'])
```

**证据引用：** `local-rule:bfbbec60c65df598`, `diff-ast:9fcb343f3efdc527`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 9. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`首板低开策略-终极版 最大回撤15%，年化50%.py:362` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
close_price = get_price(code, end_date=date, count=1, fields=['close'])['close'].iloc[0]
```

**证据引用：** `local-rule:84b1cdcf4b8a6163`, `diff-ast:c5cd6fc722e3e08a`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

