# EvoAgent 量化代码审查

**Repository:** `聚宽2025/干积分-大小盘反复横跳V2.0`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 7 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `7`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `183 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`干积分-大小盘反复横跳V2.0.py:98` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price(stock, end_date=stat_date, frequency='daily', fields=['close','high_limit'], count=recent_days, panel=False, fill_paused=False)
```

**证据引用：** `local-rule:e06ec5b11fffd35e`, `diff-ast:770734bfb064331e`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`干积分-大小盘反复横跳V2.0.py:155` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
h_ratio = get_price(stockList_2000, end_date = yesterday , frequency='1d', fields=['close'], count=20)['close']
```

**证据引用：** `local-rule:d23a2580c882fdf7`, `diff-ast:73caad53e7132283`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`干积分-大小盘反复横跳V2.0.py:160` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
h_ratio = get_price(stockList_500, end_date = yesterday , frequency='1d', fields=['close'], count=20)['close']
```

**证据引用：** `local-rule:6b70343840133984`, `diff-ast:ee1156f4328ede59`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🟠 set_order_cost 佣金/税费设为 0，回测收益虚高

`干积分-大小盘反复横跳V2.0.py:24` · **MEDIUM** · `QUANT-EX-ZERO-ORDERCOST`

聚宽 set_order_cost 将佣金或税费设为 0 会高估策略真实收益，实盘存在成本。

**证据**

```text
set_order_cost(OrderCost(open_tax=0, close_tax=0.001, open_commission=0.0003, close_commission=0.0003,
```

**证据引用：** `local-rule:2745baffad38561c`

**修复建议：** 按真实券商费率设置 open_commission/close_commission/close_tax。

**测试建议：** 对比零成本与真实成本下的收益与回撤差异。

### 5. 🟠 假设零手续费（set_commission 缺失/为 0）

`干积分-大小盘反复横跳V2.0.py:25` · **MEDIUM** · `QUANT-EX-ZERO-COMM`

手续费为 0 会高估高频/换手策略收益。

**证据**

```text
close_today_commission=0, min_commission=5), type='stock')
```

**证据引用：** `local-rule:67e2348a4cf6dfb5`

**修复建议：** 设置与券商一致的佣金、印花税与最低手续费。

**测试建议：** 断言手续费计入后收益曲线符合预期。

### 6. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`干积分-大小盘反复横跳V2.0.py:153` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList_2000=get_index_stocks('399303.XSHE',now_time)
```

**证据引用：** `local-rule:4ff0fe1bc527c4cf`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 7. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`干积分-大小盘反复横跳V2.0.py:154` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList_500=get_index_stocks('399905.XSHE',now_time)
```

**证据引用：** `local-rule:caf3b1dde9658a39`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

