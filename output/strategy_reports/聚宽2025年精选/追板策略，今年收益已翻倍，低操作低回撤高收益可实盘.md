# EvoAgent 量化代码审查

**Repository:** `聚宽2025/追板策略，今年收益已翻倍，低操作低回撤高收益可实盘`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 6 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `8`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `203 ms`

## 审查发现

### 1. 🔴 策略逻辑使用了真实当前时间

`追板策略，今年收益已翻倍，低操作低回撤高收益可实盘.py:41` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
g.excel_name = 'myFile/追首日涨停/' + str(context.current_dt)[:10] + ' by ' + str(datetime.now())[:-7] + '.csv'
```

**证据引用：** `local-rule:3e7611b30bcd2a20`, `diff-ast:9c7b33cd8885278e`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 2. 🔴 策略逻辑使用了真实当前时间

`追板策略，今年收益已翻倍，低操作低回撤高收益可实盘.py:43` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
g.excel_name = 'myFile/追首日涨停/实盘日志 by ' + str(datetime.now())[:-7] + '.csv'
```

**证据引用：** `local-rule:7b1fca02d771e8dd`, `diff-ast:7ce45bea68014788`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`追板策略，今年收益已翻倍，低操作低回撤高收益可实盘.py:377` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
high_prices = get_price(s, count=100, end_date=theDay, fields=['high'], skip_paused=True)['high']
```

**证据引用：** `local-rule:29ec6300954803f4`, `diff-ast:1ed2129c2c791141`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`追板策略，今年收益已翻倍，低操作低回撤高收益可实盘.py:381` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
volume_data = get_price(s, end_date=theDay, count=zyts, frequency='1d', fields=['volume'], skip_paused=True)
```

**证据引用：** `local-rule:6487bc2c37ef192e`, `diff-ast:f038678893f81ce2`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🟠 set_order_cost 佣金/税费设为 0，回测收益虚高

`追板策略，今年收益已翻倍，低操作低回撤高收益可实盘.py:30` · **MEDIUM** · `QUANT-EX-ZERO-ORDERCOST`

聚宽 set_order_cost 将佣金或税费设为 0 会高估策略真实收益，实盘存在成本。

**证据**

```text
set_order_cost(OrderCost(open_tax=0, close_tax=0.0005, open_commission=0.0001, close_commission=0.0001,
```

**证据引用：** `local-rule:f05a8b5494a12e54`

**修复建议：** 按真实券商费率设置 open_commission/close_commission/close_tax。

**测试建议：** 对比零成本与真实成本下的收益与回撤差异。

### 6. 🟠 假设零手续费（set_commission 缺失/为 0）

`追板策略，今年收益已翻倍，低操作低回撤高收益可实盘.py:31` · **MEDIUM** · `QUANT-EX-ZERO-COMM`

手续费为 0 会高估高频/换手策略收益。

**证据**

```text
close_today_commission=0, min_commission=5), type='stock')
```

**证据引用：** `local-rule:72e0e8f866183b8a`

**修复建议：** 设置与券商一致的佣金、印花税与最低手续费。

**测试建议：** 断言手续费计入后收益曲线符合预期。

