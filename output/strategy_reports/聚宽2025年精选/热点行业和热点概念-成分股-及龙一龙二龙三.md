# EvoAgent 量化代码审查

**Repository:** `聚宽2025/热点行业和热点概念-成分股-及龙一龙二龙三`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 11 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `18`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `244 ms`

## 审查发现

### 1. 🔴 策略逻辑使用了真实当前时间

`热点行业和热点概念-成分股-及龙一龙二龙三.py:34` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
program_start_dt = dt.datetime.now()
```

**证据引用：** `local-rule:9c9bfdfc32671605`, `diff-ast:22439bedc6301e79`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`热点行业和热点概念-成分股-及龙一龙二龙三.py:327` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
prices = get_price(stock_list, end_date=end_dt, count=count + return_days, fields=['pre_close', 'high'], panel=False)
```

**证据引用：** `local-rule:04c63ec6f60948a1`, `diff-ast:b85a4cc49b6569cf`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`热点行业和热点概念-成分股-及龙一龙二龙三.py:411` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
close = get_price(stock_list, end_date=end_date, count=count + ma_len + 1, fields=['close'])['close']
```

**证据引用：** `local-rule:0ce6faa91f8e7d87`, `diff-ast:ad2d618a9d2d5f33`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 策略逻辑使用了真实当前时间

`热点行业和热点概念-成分股-及龙一龙二龙三.py:854` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
close_prices = get_price(concept_stocks_formatted, end_date=dt.datetime.now(), count=count + 1, fields=['close'], panel=True)['close']
```

**证据引用：** `local-rule:ce65ac889713e093`, `diff-ast:d21e466b136f83e6`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 5. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`热点行业和热点概念-成分股-及龙一龙二龙三.py:947` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
close_prices = get_price(concept_stocks, end_date=end_date, count=watch_days + 1, fields=['close'], panel=True)['close']
```

**证据引用：** `local-rule:d8321c6723abe50c`, `diff-ast:01e4c0d955e18e16`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 6. 🟡 用 0 填补缺失值可能扭曲收益率

`热点行业和热点概念-成分股-及龙一龙二龙三.py:452` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
df_category_score = pd.DataFrame.from_dict(dat_cat_dict, orient='index').fillna(0).tail(count).T
```

**证据引用：** `local-rule:f9922a4543a8a334`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 7. 🟡 用 0 填补缺失值可能扭曲收益率

`热点行业和热点概念-成分股-及龙一龙二龙三.py:531` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
df_category_score = pd.DataFrame.from_dict(dat_cat_dict, orient='index').fillna(0).tail(count).T
```

**证据引用：** `local-rule:6ce6a15976604b82`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 8. 🟡 用 0 填补缺失值可能扭曲收益率

`热点行业和热点概念-成分股-及龙一龙二龙三.py:957` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
close_prices = close_prices.fillna(method='ffill').fillna(0)
```

**证据引用：** `local-rule:28128e63212c7664`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 9. 🟡 非交互式回测中调用 plt.show()

`热点行业和热点概念-成分股-及龙一龙二龙三.py:1017` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:热点行业和热点概念-成分股-及龙一龙二龙三.py:1017`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 10. 🟡 非交互式回测中调用 plt.show()

`热点行业和热点概念-成分股-及龙一龙二龙三.py:1650` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:热点行业和热点概念-成分股-及龙一龙二龙三.py:1650`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

### 11. 🟡 非交互式回测中调用 plt.show()

`热点行业和热点概念-成分股-及龙一龙二龙三.py:1713` · **LOW** · `QUANT-CQ-DEBUG-PLOT`

plt.show() 在无人值守的回测/调度环境会阻塞等待窗口关闭，使任务挂起。

**证据**

```text
plt.show()
```

**证据引用：** `scanner:QUANT-CQ-DEBUG-PLOT:热点行业和热点概念-成分股-及龙一龙二龙三.py:1713`

**修复建议：** 改用 plt.savefig 或将绘图移到独立的可视化脚本。

**测试建议：** 验证批处理模式下不会弹出窗口或阻塞进程。

