# EvoAgent 量化代码审查

**Repository:** `聚宽2025/基金溢价（模拟效果好！）`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 2 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `5`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `163 ms`

## 审查发现

### 1. 🔴 策略逻辑使用了真实当前时间

`基金溢价（模拟效果好！）.py:77` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
today = datetime.datetime.now().date()
```

**证据引用：** `local-rule:e56157ebe6e2b5f9`, `diff-ast:bba5ad7cbc5d65b7`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 2. 🟠 假设零手续费（set_commission 缺失/为 0）

`基金溢价（模拟效果好！）.py:36` · **MEDIUM** · `QUANT-EX-ZERO-COMM`

手续费为 0 会高估高频/换手策略收益。

**证据**

```text
close_commission=0.00025, close_today_commission=0, min_commission=5),
```

**证据引用：** `local-rule:2695b9ccf66eca48`

**修复建议：** 设置与券商一致的佣金、印花税与最低手续费。

**测试建议：** 断言手续费计入后收益曲线符合预期。

