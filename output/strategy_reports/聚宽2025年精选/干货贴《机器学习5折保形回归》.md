# EvoAgent 量化代码审查

**Repository:** `聚宽2025/干货贴《机器学习5折保形回归》`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 14 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `9`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `157 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`干货贴《机器学习5折保形回归》.py:35` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
stock_data = get_price('000001.XSHE',start_date,end_date,'daily',fields=['close'])
```

**证据引用：** `local-rule:b4c0d85286510552`, `diff-ast:a9ab8de870deacac`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`干货贴《机器学习5折保形回归》.py:177` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
data_close=get_price(stockList,date,dateList[dateList.index(date)+1],'1d','close')['close']
```

**证据引用：** `local-rule:d7030de476f388f0`, `diff-ast:14371f3e4479d093`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`干货贴《机器学习5折保形回归》.py:180` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
SZ=get_price('000001.XSHG',date,dateList[dateList.index(date)+1],'1d','close')['close']
```

**证据引用：** `local-rule:02c55f413d0bc762`, `diff-ast:e0c1fd1e46191f97`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`干货贴《机器学习5折保形回归》.py:215` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
data_close=get_price(stockList,date,dateList[dateList.index(date)+1],'1d','close')['close']
```

**证据引用：** `local-rule:1bfe2f7cd77ba0f2`, `diff-ast:9ea18493b680a5d7`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`干货贴《机器学习5折保形回归》.py:218` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
SZ=get_price('000001.XSHG',date,dateList[dateList.index(date)+1],'1d','close')['close']
```

**证据引用：** `local-rule:b2c8ba8c9eae6ffc`, `diff-ast:57053df92bbe2397`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 6. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`干货贴《机器学习5折保形回归》.py:77` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('000300.XSHG',begin_date)
```

**证据引用：** `local-rule:e6bb12a3a670dad7`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 7. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`干货贴《机器学习5折保形回归》.py:79` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('399905.XSHE',begin_date)
```

**证据引用：** `local-rule:614672abbbfeeb30`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 8. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`干货贴《机器学习5折保形回归》.py:81` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('399906.XSHE',begin_date)
```

**证据引用：** `local-rule:259eecbca2ace8cd`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 9. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`干货贴《机器学习5折保形回归》.py:83` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('399006.XSHE',begin_date)
```

**证据引用：** `local-rule:9682cc863c866968`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 10. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`干货贴《机器学习5折保形回归》.py:85` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('399005.XSHE',begin_date)
```

**证据引用：** `local-rule:ff2d1d0741a192a4`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 11. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`干货贴《机器学习5折保形回归》.py:87` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('000002.XSHG',begin_date)+get_index_stocks('399107.XSHE',begin_date)
```

**证据引用：** `local-rule:6dbc4b7ce8d71ac7`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 12. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`干货贴《机器学习5折保形回归》.py:90` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
stockList=get_index_stocks('000985.XSHG',begin_date)
```

**证据引用：** `local-rule:0f1276c5e52b1481`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 13. 🟠 裸异常吞掉错误

`干货贴《机器学习5折保形回归》.py:193` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:785d98b5354c3c75`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

### 14. 🟠 裸异常吞掉错误

`干货贴《机器学习5折保形回归》.py:230` · **MEDIUM** · `QUANT-REL-NO-EXCEPTION`

裸 except 或 except Exception 后不处理、不记录，会掩盖数据或交易接口的真实错误，难以排查。

**证据**

```text
except:
```

**证据引用：** `local-rule:1c5668459441077a`

**修复建议：** 捕获具体异常类型并记录错误上下文，或至少 log.exception。

**测试建议：** 构造异常输入，断言错误被记录且任务状态可见。

