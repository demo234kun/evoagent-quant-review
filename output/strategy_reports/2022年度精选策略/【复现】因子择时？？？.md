# EvoAgent 量化代码审查

**Repository:** `聚宽2022/【复现】因子择时？？？`  
**Risk:** `low`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 4 个可处理问题。整体风险：low。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `4`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `161 ms`

## 审查发现

### 1. 🟡 用 0 填补缺失值可能扭曲收益率

`【复现】因子择时？？？.py:144` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
r2 = rolling_R2(g.df_m_shifted_.fillna(0),g.Ret_mat.fillna(0),window=24)  #计算拟合优度
```

**证据引用：** `local-rule:0577dbb7d7ad4913`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 2. 🟡 用 0 填补缺失值可能扭曲收益率

`【复现】因子择时？？？.py:151` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
r2 = rolling_R2(g.df_m_shifted_.fillna(0),g.Ret_mat.fillna(0),window=24)  #计算拟合优度
```

**证据引用：** `local-rule:55eb81d9fe1dcb41`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 3. 🟡 用 0 填补缺失值可能扭曲收益率

`【复现】因子择时？？？.py:294` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
regr.fit(x_train.fillna(0), y_design_logit.astype(float))
```

**证据引用：** `local-rule:5afb260140ea3eaf`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

### 4. 🟡 用 0 填补缺失值可能扭曲收益率

`【复现】因子择时？？？.py:295` · **LOW** · `QUANT-DQ-FILLNA0`

价格/因子缺失填 0 会产生异常收益率并污染信号。

**证据**

```text
predict.iloc[i] = regr.predict(x_test.fillna(0))
```

**证据引用：** `local-rule:1a52d92f4dbb1ed1`

**修复建议：** 用 ffill、插值或丢弃缺失，并在缺失处理时只用历史信息。

**测试建议：** 构造含缺失的用例，断言缺失行未被填成不合理数值。

