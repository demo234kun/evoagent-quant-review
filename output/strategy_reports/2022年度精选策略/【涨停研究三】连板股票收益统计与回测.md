# EvoAgent 量化代码审查

**Repository:** `聚宽2022/【涨停研究三】连板股票收益统计与回测`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 5 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `9`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `149 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`【涨停研究三】连板股票收益统计与回测.py:41` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price(stock_list, end_date=stat_date, frequency='daily', fields=['close','high_limit'], count=watch_days, panel=False, fill_paused=False)
```

**证据引用：** `local-rule:d47729c5b915a9bf`, `diff-ast:40e5346b995d0514`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`【涨停研究三】连板股票收益统计与回测.py:44` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price(stock_list, end_date=stat_date, frequency='daily', fields=['close','low_limit'], count=watch_days, panel=False, fill_paused=False)
```

**证据引用：** `local-rule:9f2c93ffffca1d7b`, `diff-ast:66ad0b9637c2594e`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`【涨停研究三】连板股票收益统计与回测.py:67` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price(security=stock, end_date=watch_date, fields=['open'], count=1, panel=False)
```

**证据引用：** `local-rule:d7929b0d4d11b3e0`, `diff-ast:f85bfa6127707d87`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`【涨停研究三】连板股票收益统计与回测.py:70` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price(security=stock, end_date=last_keep_date, fields=['close'], count=1, panel=False)
```

**证据引用：** `local-rule:4aa73985b902fdfc`, `diff-ast:f3eca3c0d2ca43b3`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`【涨停研究三】连板股票收益统计与回测.py:73` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price(security=stock, start_date=sellable_date, end_date=last_keep_date, fields=['high','low'], panel=False)
```

**证据引用：** `local-rule:24b9537591f6d33a`, `diff-ast:6c4ed3ec7b88b69b`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

