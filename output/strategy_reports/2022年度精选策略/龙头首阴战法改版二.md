# EvoAgent 量化代码审查

**Repository:** `聚宽2022/龙头首阴战法改版二`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 22 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `27`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `173 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:85` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel = get_price(stock, count = 1,end_date=pre_date, frequency='daily', fields=['open', 'high', 'close','low', 'high_limit','money'])
```

**证据引用：** `local-rule:7e0f740501fcd5ac`, `diff-ast:3144b1e4a69c9791`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 行情接口以当前时刻为结束日（包含未发生的 bar）

`龙头首阴战法改版二.py:126` · **HIGH** · `QUANT-FF-GETPRICE-NOW`

JoinQuant 的 get_price/attribute_history 在 end_date=context.current_dt 时会把当根 bar 也算入，若据此下单即为未来函数。

**证据**

```text
df = get_price(stock_two, start_date=context.portfolio.positions[stock_two].init_time,end_date=context.current_dt, frequency='minute', fields=['close'],skip_paused=True)
```

**证据引用：** `local-rule:bab1f623faf08b46`, `diff-ast:d483a8e6f93ccd45`

**修复建议：** 用 context.previous_date 作为结束日，或改用 history()（默认不含当根）。

**测试建议：** 断言行情查询的结束日早于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:131` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel = get_price(stock_two, count = 1,end_date=pre_date, frequency='daily', fields=['open', 'close','high_limit','money','low',])
```

**证据引用：** `local-rule:71f00fc5e50df9ac`, `diff-ast:6a0432280e8ee738`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:134` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_2 = get_price(stock_two, count = 2,end_date=pre_date, frequency='daily', fields=['high', 'close','high_limit','money'],skip_paused=True)
```

**证据引用：** `local-rule:1335cc80f25df557`, `diff-ast:940e8686375015c9`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🔴 行情接口以当前时刻为结束日（包含未发生的 bar）

`龙头首阴战法改版二.py:175` · **HIGH** · `QUANT-FF-GETPRICE-NOW`

JoinQuant 的 get_price/attribute_history 在 end_date=context.current_dt 时会把当根 bar 也算入，若据此下单即为未来函数。

**证据**

```text
df = get_price(stock_two, start_date=context.portfolio.positions[stock_two].init_time,end_date=context.current_dt, frequency='minute', fields=['high'],skip_paused=True)
```

**证据引用：** `local-rule:d79bdeb65cb3cc66`, `diff-ast:0200098ef941ae5c`

**修复建议：** 用 context.previous_date 作为结束日，或改用 history()（默认不含当根）。

**测试建议：** 断言行情查询的结束日早于当前信号 bar。

### 6. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:180` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel = get_price(stock_two, count = 1,end_date=pre_date, frequency='daily', fields=['open', 'close','high_limit','money','low',])
```

**证据引用：** `local-rule:51d8e71613d6d3c1`, `diff-ast:0186e52db9ea142c`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 7. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:186` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price(stock_two, start_date=context.portfolio.positions[stock_two].init_time,end_date=context.previous_date, frequency='minute', fields=['close'],skip_paused=True)
```

**证据引用：** `local-rule:f62d742f8adeba92`, `diff-ast:14ec59f0e8dd7ffb`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 8. 🔴 策略逻辑使用了真实当前时间

`龙头首阴战法改版二.py:215` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
date_now =  (context.current_dt + timedelta(days = -1)).strftime("%Y-%m-%d")#'2021-01-15'#datetime.datetime.now()
```

**证据引用：** `local-rule:261b4c7fc1b31090`, `diff-ast:2849925bc4d77573`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 9. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:231` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel = get_price(stock, count = 1,end_date=stock_date, frequency='daily', fields=['open', 'close','high_limit','money','low','high','pre_close'])
```

**证据引用：** `local-rule:25b1ebb9f48f5df1`, `diff-ast:8d3b8e4e380bcdb9`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 10. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:238` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_8 = get_price(stock, count = 8,end_date=end_date, frequency='daily', fields=['high', 'close','high_limit','money'],skip_paused=True)
```

**证据引用：** `local-rule:65c65f81666aed52`, `diff-ast:36b6711bfd0c8446`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 11. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:250` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_30 = get_price(stock, count = 30,end_date=pre_date_six, frequency='daily', fields=['high', 'close','high_limit','money'],skip_paused=True)
```

**证据引用：** `local-rule:b39e1f5ba5e496c3`, `diff-ast:956dc80ec38f331a`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 12. 🔴 策略逻辑使用了真实当前时间

`龙头首阴战法改版二.py:260` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
date_now =  (context.current_dt + timedelta(days = -1)).strftime("%Y-%m-%d")#'2021-01-15'#datetime.datetime.now()
```

**证据引用：** `local-rule:90a599d446b6b048`, `diff-ast:03ffd99ea528e621`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 13. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:262` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_close = get_price(stock, count = 1,end_date=date_now, frequency='daily', fields=['open', 'close','high','money'])
```

**证据引用：** `local-rule:83bdf506d43dc156`, `diff-ast:bb58954af217c204`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 14. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:281` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel = get_price(stock, count = 1,end_date=pre_date, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:f165d23e65670e78`, `diff-ast:a9a5b1c13201c568`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 15. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:285` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_two = get_price(stock, count = 1,end_date=pre_date_two, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:29a3285d5aa9e0c4`, `diff-ast:88b465fc5b46a574`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 16. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:296` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_pre = get_price(stock, count = 1,end_date=pre_trade_date_break, frequency='daily', fields=['open', 'close','high_limit','money','pre_close'])
```

**证据引用：** `local-rule:c0f17d5cba4f47b7`, `diff-ast:8f471966ace28a18`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 17. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:303` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_all = get_price(stock, count = 250,end_date=trade_date_break, frequency='daily', fields=['high', 'close','high_limit','money'])
```

**证据引用：** `local-rule:a935cc376ff628ef`, `diff-ast:2186bb765cefad80`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 18. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:309` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_30 = get_price(stock, count = 30,end_date=trade_date_break, frequency='daily', fields=['high', 'close','high_limit','money'],skip_paused=True)
```

**证据引用：** `local-rule:e5e7fa524e33627a`, `diff-ast:9a4ac00e4239b23d`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 19. 🔴 策略逻辑使用了真实当前时间

`龙头首阴战法改版二.py:323` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
date_now =  (context.current_dt+ timedelta(days = -1)).strftime("%Y-%m-%d")#'2021-01-15'#datetime.datetime.now()
```

**证据引用：** `local-rule:0c2043cf2fbc6c56`, `diff-ast:19d023fa12b679c5`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 20. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:328` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel = get_price(stock, count = 1,end_date=datenum, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:b2e155bb343aaeb8`, `diff-ast:7481c5af391ce7fd`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 21. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:344` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel = get_price(stock, count = 1,end_date=datenum, frequency='daily', fields=['open', 'close','high_limit','money','pre_close'])
```

**证据引用：** `local-rule:25913da256099e5b`, `diff-ast:466b3dad706976ff`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 22. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`龙头首阴战法改版二.py:354` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel = get_price(stocks, count = 1,end_date=pre_date, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:12a865d990393524`, `diff-ast:1ea9b5dba28da363`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

