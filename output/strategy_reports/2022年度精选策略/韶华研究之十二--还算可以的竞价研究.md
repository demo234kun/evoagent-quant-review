# EvoAgent 量化代码审查

**Repository:** `聚宽2022/韶华研究之十二--还算可以的竞价研究`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 5 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `9`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `161 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`韶华研究之十二--还算可以的竞价研究.py:203` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_price=get_price(stocklist, count=duration, end_date=check_date, skip_paused=True, panel=False)
```

**证据引用：** `local-rule:2c25cb41e09bd7fd`, `diff-ast:507b9bec52fe449f`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`韶华研究之十二--还算可以的竞价研究.py:219` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_price = get_price(stockcode,end_date=check_date,frequency='daily',fields=['pre_close','open','high_limit','high','low','close'],count=2)
```

**证据引用：** `local-rule:0ef9aaf1abaff05f`, `diff-ast:407e7af276a57f00`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`韶华研究之十二--还算可以的竞价研究.py:223` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_price = get_price(stockcode,end_date=check_date,frequency='daily',fields=['pre_close','open','high_limit','high','low','close','volume'],count=41)
```

**证据引用：** `local-rule:abb5906d871a867f`, `diff-ast:3b8daa55a3cccb1a`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`韶华研究之十二--还算可以的竞价研究.py:252` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_price = get_price(stockcode,end_date=lastd_date,frequency='daily',fields=['close'],count=7)
```

**证据引用：** `local-rule:3ca181e132073c7a`, `diff-ast:bfa5bba24a8ad4f1`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`韶华研究之十二--还算可以的竞价研究.py:290` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_price = get_price(stockcode,end_date=lastd_date,frequency='daily',fields=['pre_close','high_limit','low_limit','high','low','close'],count=60)
```

**证据引用：** `local-rule:4be50746a2a0e96b`, `diff-ast:dcf709720a834451`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

