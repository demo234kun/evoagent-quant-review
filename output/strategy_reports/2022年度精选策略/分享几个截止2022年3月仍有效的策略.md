# EvoAgent 量化代码审查

**Repository:** `聚宽2022/分享几个截止2022年3月仍有效的策略`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 12 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `9`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `177 ms`

## 审查发现

### 1. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`分享几个截止2022年3月仍有效的策略.py:230` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df = get_price(MKT_list, end_date=stat_date, frequency='daily', fields=['close','high_limit'], count=watch_days, panel=False, fill_paused=True)
```

**证据引用：** `local-rule:ed4d0850dbf0b012`, `diff-ast:9f183d36b4b604a6`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`分享几个截止2022年3月仍有效的策略.py:255` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
close_now = get_price(ref, end_date=stat_date, frequency='daily', fields=['close'], skip_paused=False, fq='pre', count=1, panel=False, fill_paused=True).iloc[-1,0]
```

**证据引用：** `local-rule:0d4a1601797f39b5`, `diff-ast:f0306e77bd7a219e`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`分享几个截止2022年3月仍有效的策略.py:256` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
close_past = get_price(ref, end_date=past_date, frequency='daily', fields=['close'], skip_paused=False, fq='pre', count=1, panel=False, fill_paused=True).iloc[-1,0]
```

**证据引用：** `local-rule:9136621d29585df8`, `diff-ast:6fa7c96d5b7b3976`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`分享几个截止2022年3月仍有效的策略.py:259` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
close_now = get_price(stock, end_date=stat_date, frequency='daily', fields=['close'], skip_paused=False, fq='pre', count=1, panel=False, fill_paused=True).iloc[-1,0]
```

**证据引用：** `local-rule:26b800df58250f3b`, `diff-ast:452250e2281c670a`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`分享几个截止2022年3月仍有效的策略.py:260` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
close_past = get_price(stock, end_date=past_date, frequency='daily', fields=['close'], skip_paused=False, fq='pre', count=1, panel=False, fill_paused=True).iloc[-1,0]
```

**证据引用：** `local-rule:4eee96e2a624c605`, `diff-ast:9389757e3ce271b9`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 6. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`分享几个截止2022年3月仍有效的策略.py:38` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
check_out_lists = get_index_stocks(g.security_universe_index)
```

**证据引用：** `local-rule:2e64953d30ae62a9`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 7. 🟠 假设零手续费（set_commission 缺失/为 0）

`分享几个截止2022年3月仍有效的策略.py:142` · **MEDIUM** · `QUANT-EX-ZERO-COMM`

手续费为 0 会高估高频/换手策略收益。

**证据**

```text
set_order_cost(OrderCost(open_tax=0, close_tax=0.000, open_commission=0.0000, close_commission=0.0000, close_today_commission=0, min_commission=5),type='fund')
```

**证据引用：** `local-rule:eaca0c6395b5b170`

**修复建议：** 设置与券商一致的佣金、印花税与最低手续费。

**测试建议：** 断言手续费计入后收益曲线符合预期。

### 8. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`分享几个截止2022年3月仍有效的策略.py:158` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
MKT_list = get_index_stocks(MKT_index)
```

**证据引用：** `local-rule:01b24b1aab4219ea`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 9. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`分享几个截止2022年3月仍有效的策略.py:229` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
MKT_list = get_index_stocks(MKT_index)
```

**证据引用：** `local-rule:91c63cb4b4f73e43`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 10. 🟠 取指数成分股未指定 date，默认当前成分（幸存者偏差）

`分享几个截止2022年3月仍有效的策略.py:246` · **MEDIUM** · `QUANT-LK-INDEX-NOW`

不传 date 时取到的是当前成分股，已剔除退市/调出股票，回测会幸存者偏差。

**证据**

```text
MKT_list = get_index_stocks(MKT_index)
```

**证据引用：** `local-rule:46bfcbc3a59964d5`

**修复建议：** 传入回测对应的历史 date 参数获取当时成分股。

**测试建议：** 对比传入历史 date 与默认结果的成分股数量与标的差异。

### 11. 🟠 假设零手续费（set_commission 缺失/为 0）

`分享几个截止2022年3月仍有效的策略.py:331` · **MEDIUM** · `QUANT-EX-ZERO-COMM`

手续费为 0 会高估高频/换手策略收益。

**证据**

```text
set_order_cost(OrderCost(open_tax=0, close_tax=0.001, open_commission=0.0003, close_commission=0.0003, close_today_commission=0, min_commission=5),type='fund')
```

**证据引用：** `local-rule:54cbcc972096f466`

**修复建议：** 设置与券商一致的佣金、印花税与最低手续费。

**测试建议：** 断言手续费计入后收益曲线符合预期。

### 12. 🟠 假设零手续费（set_commission 缺失/为 0）

`分享几个截止2022年3月仍有效的策略.py:525` · **MEDIUM** · `QUANT-EX-ZERO-COMM`

手续费为 0 会高估高频/换手策略收益。

**证据**

```text
set_order_cost(OrderCost(open_tax=0, close_tax=0.001, open_commission=0.0003, close_commission=0.0003, close_today_commission=0, min_commission=5),type='fund')
```

**证据引用：** `local-rule:873821f959bb613c`

**修复建议：** 设置与券商一致的佣金、印花税与最低手续费。

**测试建议：** 断言手续费计入后收益曲线符合预期。

