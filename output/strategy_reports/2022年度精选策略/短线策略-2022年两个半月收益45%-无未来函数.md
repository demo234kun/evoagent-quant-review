# EvoAgent 量化代码审查

**Repository:** `聚宽2022/短线策略-2022年两个半月收益45%-无未来函数`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 7 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `10`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `145 ms`

## 审查发现

### 1. 🔴 策略逻辑使用了真实当前时间

`短线策略-2022年两个半月收益45%-无未来函数.py:44` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
start = datetime.now()
```

**证据引用：** `local-rule:4d7066d54e139ff8`, `diff-ast:050c8e1be6f05093`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 2. 🔴 策略逻辑使用了真实当前时间

`短线策略-2022年两个半月收益45%-无未来函数.py:63` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
end = datetime.now()
```

**证据引用：** `local-rule:79da02b5d240ae13`, `diff-ast:e4553f423606e653`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 3. 🔴 策略逻辑使用了真实当前时间

`短线策略-2022年两个半月收益45%-无未来函数.py:74` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
start = datetime.now()
```

**证据引用：** `local-rule:f6e562434975e62a`, `diff-ast:9cc5a794677a36dc`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 4. 🔴 策略逻辑使用了真实当前时间

`短线策略-2022年两个半月收益45%-无未来函数.py:111` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
end = datetime.now()
```

**证据引用：** `local-rule:e65e99b47ce9544d`, `diff-ast:f837549cead08464`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 5. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`短线策略-2022年两个半月收益45%-无未来函数.py:199` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
price_raw=get_price(security=stock_pool, end_date=cur_date, frequency='daily', fields=['close','volume'], count=31, panel=False,skip_paused=False)
```

**证据引用：** `local-rule:5e1706a923f2dc33`, `diff-ast:c557f011749a875d`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 6. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`短线策略-2022年两个半月收益45%-无未来函数.py:285` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
price=get_price(security=stock_pool, end_date=cur_date, frequency='1d', fields=['close'], count=1,panel=False, skip_paused=False)[['code','close']]
```

**证据引用：** `local-rule:43fbbccc0d46bd0f`, `diff-ast:b3dae5b2d91cab77`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 7. 🟠 假设零手续费（set_commission 缺失/为 0）

`短线策略-2022年两个半月收益45%-无未来函数.py:32` · **MEDIUM** · `QUANT-EX-ZERO-COMM`

手续费为 0 会高估高频/换手策略收益。

**证据**

```text
set_order_cost(OrderCost(open_tax=0, close_tax=0.001, open_commission=0.0003, close_commission=0.0003, close_today_commission=0, min_commission=5),type='fund')
```

**证据引用：** `local-rule:8a62aba9f0685717`

**修复建议：** 设置与券商一致的佣金、印花税与最低手续费。

**测试建议：** 断言手续费计入后收益曲线符合预期。

