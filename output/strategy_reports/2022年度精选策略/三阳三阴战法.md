# EvoAgent 量化代码审查

**Repository:** `聚宽2022/三阳三阴战法`  
**Risk:** `high`  
**Reviewer:** `mode-router`

已审查 1 个文件；发现 32 个可处理问题。整体风险：high。

## 执行概况

- 模式：请求 `rules-only`，实际 `rules-only`
- 模型调用: `0`；工具调用: `36`
- Token：输入 `0`、输出 `0`、合计 `0`
- Token 成本: `$0.00000000`；延迟: `206 ms`

## 审查发现

### 1. 🔴 策略逻辑使用了真实当前时间

`三阳三阴战法.py:35` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
date_now =  (context.current_dt + timedelta(days = -1)).strftime("%Y-%m-%d")#'2021-01-15'#datetime.datetime.now()
```

**证据引用：** `local-rule:6e62ba01fd2b8446`, `diff-ast:b3aaaac04c041f80`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 2. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:45` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_yes = get_price(stock, count = 1,end_date=pre_date_yes, frequency='daily', fields=['open', 'close','high_limit','money','low','high'])
```

**证据引用：** `local-rule:a257cf557209b141`, `diff-ast:ace5165c001e1674`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 3. 🔴 行情接口以当前时刻为结束日（包含未发生的 bar）

`三阳三阴战法.py:71` · **HIGH** · `QUANT-FF-GETPRICE-NOW`

JoinQuant 的 get_price/attribute_history 在 end_date=context.current_dt 时会把当根 bar 也算入，若据此下单即为未来函数。

**证据**

```text
df = get_price(stock_two, start_date=lastToday,end_date=context.current_dt, frequency='minute', fields=['high','low'],skip_paused=True)
```

**证据引用：** `local-rule:5988e0cba29a2b99`, `diff-ast:3f2fc265f7bbe611`

**修复建议：** 用 context.previous_date 作为结束日，或改用 history()（默认不含当根）。

**测试建议：** 断言行情查询的结束日早于当前信号 bar。

### 4. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:76` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel = get_price(stock_two, count = 1,end_date=pre_date, frequency='daily', fields=['open', 'close','high_limit','money','low',])
```

**证据引用：** `local-rule:e680c72895d35d86`, `diff-ast:59d2e3bb723cd673`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 5. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:89` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_3 = get_price(stock_two, count = 3,end_date=pre_date, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:5e762d8d42a7d25f`, `diff-ast:33cfb6fab949e49f`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 6. 🔴 行情接口以当前时刻为结束日（包含未发生的 bar）

`三阳三阴战法.py:119` · **HIGH** · `QUANT-FF-GETPRICE-NOW`

JoinQuant 的 get_price/attribute_history 在 end_date=context.current_dt 时会把当根 bar 也算入，若据此下单即为未来函数。

**证据**

```text
df = get_price(stock_two, start_date=context.portfolio.positions[stock_two].init_time,end_date=context.current_dt, frequency='minute', fields=['high'],skip_paused=True)
```

**证据引用：** `local-rule:9abc75532e8f0812`, `diff-ast:68b6225c60c63a14`

**修复建议：** 用 context.previous_date 作为结束日，或改用 history()（默认不含当根）。

**测试建议：** 断言行情查询的结束日早于当前信号 bar。

### 7. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:123` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel = get_price(stock_two, count = 1,end_date=pre_date, frequency='daily', fields=['open', 'close','high_limit','money','low',])
```

**证据引用：** `local-rule:2127c74605f5e8fa`, `diff-ast:626e81973c9f10fb`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 8. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:127` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_5 = get_price(stock_two, count = 5,end_date=pre_date, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:5cb39e32da84a4db`, `diff-ast:95517b1a5b4b3d31`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 9. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:130` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_10 = get_price(stock_two, count = 10,end_date=pre_date, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:71f684e82f4c38e1`, `diff-ast:b17e70a9a6e31361`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 10. 🔴 行情接口以当前时刻为结束日（包含未发生的 bar）

`三阳三阴战法.py:136` · **HIGH** · `QUANT-FF-GETPRICE-NOW`

JoinQuant 的 get_price/attribute_history 在 end_date=context.current_dt 时会把当根 bar 也算入，若据此下单即为未来函数。

**证据**

```text
df_panel_allday = get_price(stock_two, start_date=lastToday, end_date=context.current_dt, frequency='minute', fields=['high','low','close','high_limit','money'])
```

**证据引用：** `local-rule:fb3b404842032ad6`, `diff-ast:6b8183778f861872`

**修复建议：** 用 context.previous_date 作为结束日，或改用 history()（默认不含当根）。

**测试建议：** 断言行情查询的结束日早于当前信号 bar。

### 11. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:142` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_3 = get_price(stock_two, count = 3,end_date=pre_date, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:399fd707d1e7ab99`, `diff-ast:18f2ae6f3848fda2`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 12. 🔴 策略逻辑使用了真实当前时间

`三阳三阴战法.py:171` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
date_now =  (context.current_dt + timedelta(days = -1)).strftime("%Y-%m-%d")#'2021-01-15'#datetime.datetime.now()
```

**证据引用：** `local-rule:85bbba85570b6386`, `diff-ast:eb79b2d4a754fdb6`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 13. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:192` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel = get_price(stocks, count = 1,end_date=end_date, frequency='daily', fields=['open', 'close','high_limit','money','low','pre_close'])
```

**证据引用：** `local-rule:248cc30e1a912097`, `diff-ast:c300c1608c765c09`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 14. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:208` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_1 = get_price(stock, count = 1,end_date=end_date, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:e1d373c168b7ba5e`, `diff-ast:d0696692544b6cf1`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 15. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:211` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_3 = get_price(stock, count = 3,end_date=end_date, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:30e2fbe1c154caf2`, `diff-ast:cfd2a0693ad406c1`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 16. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:214` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_6 = get_price(stock, count = 6,end_date=end_date, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:3851f49061fa4d17`, `diff-ast:33988bb6a7e1d66b`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 17. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:219` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_30 = get_price(stock, count = 30,end_date=yes_date_two, frequency='daily', fields=['high','open', 'close','high_limit','low'])
```

**证据引用：** `local-rule:8dfc99d1e029d530`, `diff-ast:39a919605020d93b`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 18. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:232` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_high = get_price(stock, start_date=pre_date_high, end_date=end_date, frequency='daily', fields=['high','open', 'close','high_limit','low'])
```

**证据引用：** `local-rule:421daf166b2ca012`, `diff-ast:fefe8b97f0ae2bf2`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 19. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:235` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_5 = get_price(stock, count = 6,end_date=time_high, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:350205f6c944f06f`, `diff-ast:4010d3baf61c92d3`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 20. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:238` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_12 = get_price(stock, count = 12,end_date=time_high, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:b56b2f9ed97b52bf`, `diff-ast:43c48e98f72c3417`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 21. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:244` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_25 = get_price(stock, count = 25,end_date=yes_date_25, frequency='daily', fields=['high','open', 'close','high_limit','low'])
```

**证据引用：** `local-rule:f730da439c1db3fd`, `diff-ast:bf844ec81d60709f`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 22. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:257` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_500 = get_price(stock, count = 380,end_date=yes_date_30, frequency='daily', fields=['high','open', 'close','high_limit','low'])
```

**证据引用：** `local-rule:0d1021b84bb2f1b8`, `diff-ast:990ec7fbca075899`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 23. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:274` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_60 = get_price(stock, count = 60,end_date=date_now, frequency='daily', fields=['open', 'high', 'close','low', 'high_limit','money'])
```

**证据引用：** `local-rule:d1315307643c2bae`, `diff-ast:87119f17e5a02971`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 24. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:280` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel = get_price(stock, count = 30,end_date=date_now, frequency='daily', fields=['open', 'high', 'close','low', 'high_limit','money'])
```

**证据引用：** `local-rule:c4d58f92df3877d1`, `diff-ast:885c91bfd2b74fd0`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 25. 🔴 策略逻辑使用了真实当前时间

`三阳三阴战法.py:289` · **HIGH** · `QUANT-FF-DATETIME-NOW`

datetime.now()/today() 返回实盘运行时刻，在回测里会变成未来时刻，造成前视偏差。

**证据**

```text
pre_date =  (time_high + timedelta(days = -1)).strftime("%Y-%m-%d")#'2021-01-15'#datetime.datetime.now()
```

**证据引用：** `local-rule:b4334e117fd7860e`, `diff-ast:0a3eb90029f44409`

**修复建议：** 改用回测框架提供的当前 bar 时间（如 context.current_dt / bar.datetime）。

**测试建议：** 断言回测中读取的时间为对应 bar 的时间而非系统当前时间。

### 26. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:290` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_eight = get_price(stock, count = 10,end_date=pre_date, frequency='daily', fields=['open', 'high', 'close','low', 'high_limit','money','pre_close'])
```

**证据引用：** `local-rule:5c771ef822d7b658`, `diff-ast:471fa7ade6b51858`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 27. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:298` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_six = get_price(stock, count = 6,end_date=pre_date, frequency='daily', fields=['open', 'high', 'close','low', 'high_limit','money','pre_close'])
```

**证据引用：** `local-rule:ce9d9cfc44a7c099`, `diff-ast:8d6915174574728f`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 28. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:302` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_two = get_price(stock, count = 3,end_date=yes_date_two, frequency='daily', fields=['open', 'high', 'close','low', 'high_limit','money','pre_close'])
```

**证据引用：** `local-rule:b05c873fb205d98d`, `diff-ast:11b8d2de0b513777`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 29. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:333` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_5 = get_price(stock, count = 5,end_date=day_now, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:50d885ce7348c96d`, `diff-ast:fb4fcb5edabbd6d0`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 30. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:335` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_5_over = get_price(stock, count = 5,end_date=day_over, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:5606070bf459bb88`, `diff-ast:8e1d636f290e73c2`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 31. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:338` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_20 = get_price(stock, count = 20,end_date=day_now, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:e19b90270eb85adc`, `diff-ast:6d7a242f3f30aebe`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

### 32. 🔴 get_price 未指定结束日期，默认拉取当前时刻数据

`三阳三阴战法.py:340` · **HIGH** · `QUANT-FF-GETPRICE-NO-ENDDATE`

get_price 不传 end_date 时默认取到当前时刻，回测中等于使用了尚未发生的 bar，属于前视。

**证据**

```text
df_panel_20_over = get_price(stock, count = 20,end_date=day_over, frequency='daily', fields=['open', 'close','high_limit','money'])
```

**证据引用：** `local-rule:ab7be890a795413f`, `diff-ast:075185e8f73c5ba3`

**修复建议：** 显式传 end_date=context.previous_date 或使用 history()。

**测试建议：** 断言行情查询始终带有明确的结束日期且不晚于当前信号 bar。

